# 代码规范

本文档约束 `D:\code\ai_creative_studio` 中新增和修改的代码。目标是让正式应用保持可读、可测试和容易回滚，同时避免把隔离原型的技术选择误带入生产入口。

规则中的“必须”用于代码审查门禁；“建议”允许在变更卡中说明理由后调整。已有代码不要求一次性格式化，修改旧文件时只整理本次触及的局部，避免产生与业务无关的大面积差异。

变更卡使用 [`docs/变更卡模板.md`](docs/变更卡模板.md)，保存在 `.scratch/<feature-slug>/spec.md`。涉及 `AGENTS.md` 高风险门禁的变更，必须取得用户明确确认；其他变更由维护者在卡中完成范围、验收和回滚自检。

## 1. 技术栈与边界

### 1.1 正式应用

| 层 | 当前方案 | 约束 |
| --- | --- | --- |
| 前端框架 | 原生 HTML、CSS、JavaScript | 正式入口是 `static/index.html`、`static/app.js`、`static/styles.css`；没有评审通过的迁移变更卡时，不引入前端框架或构建步骤。 |
| 状态管理 | 页面内状态 + SQLite 服务端状态 | 当前页面以顶层 `state` 对象组织临时状态；允许随模块边界拆分视图状态，但项目、生成历史、采用关系和图片任务状态始终以 API/SQLite 为唯一事实来源。不得维护第二套长期业务事实。 |
| 样式方案 | 原生 CSS | 复用现有设计变量、组件类和响应式断点；不得引入内联样式堆叠或第三方 CSS 框架。 |
| 路由方案 | 单页、无客户端路由 | 三步工作流使用页面状态切换；API 路由集中在 `src/creative_studio/app.py`。新增页面或客户端路由前必须先说明 URL、刷新、回退和静态托管策略。 |
| 后端 | Python 标准库 HTTP 服务 + SQLite | HTTP 编排放在 `app.py`，持久化放在 `repository.py`，AI 结构与校验放在 `ai_creative.py`，图片任务放在 `image_jobs.py`。 |
| 测试 | Python `unittest` + 定向静态检查 | 测试使用临时数据库或假服务，不读取、覆盖真实 `data/`，不默认发起真实 AI 请求。 |

### 1.2 隔离原型

`frontend/` 当前只用于假数据 UI 探索，不是正式入口。其技术栈是 React 19、TypeScript、Vite、Tailwind CSS 4，并复用 Radix/shadcn 风格的本地组件。

- 状态默认使用组件局部状态、提升状态和派生值；只有出现跨多个独立页面、难以通过组合解决的共享状态时，才通过单独变更评估状态库。
- 当前没有 React Router。原型变体使用查询参数；不得把查询参数切换描述为正式路由方案。
- `frontend/` 的假数据、组件和依赖不能直接复制到正式 `static/`。提升为正式方案前，必须明确 API 接线、构建产物、Python 静态托管、回滚和浏览器兼容性。
- 直接生产依赖使用精确版本，传递依赖由提交的 lockfile 固定；新增依赖前说明用途、许可证、维护状态、体积和可替代方案。

## 2. 代码设计原则

### 2.1 职责与依赖方向

- 先确认真实入口和直接调用方，再修改代码。
- HTTP 处理器只负责解析请求、调用领域/仓储能力和形成响应，不在路由分支中堆叠 SQL、提示词拼接或图片下载逻辑。
- 仓储层封装数据库事务、约束和行到对象的转换，不依赖浏览器结构。
- AI 返回值必须先做类型、字段、数量和状态校验，再持久化或返回给页面。
- 模型输出不得未经验证直接成为 Python、PowerShell、SQL、文件路径或网络控制动作。确有业务需要时，必须增加类型校验、允许列表、路径归一化和目录边界等与用途匹配的保护。
- 优先短函数和清晰的数据流。只有出现真实复用或边界隔离价值时才抽象，不为“以后可能需要”提前建立框架。

### 2.2 前端组件与 DOM 逻辑

正式原生页面没有组件框架，按以下方式保持组件化：

- 一个渲染函数负责一个可命名的界面区域，例如项目列表、标签组或结果卡片。
- DOM 引用集中缓存；事件监听在初始化阶段集中注册，避免每次渲染重复绑定。
- 优先使用 `textContent` 和 DOM API。必须拼接 HTML 时，来自用户、数据库或 AI 的值要按文本、属性或 URL 等具体输出上下文编码；URL 还要限制允许的协议。不要把不可信内容直接写入 `innerHTML`。
- 可交互元素优先使用语义化 `button`、`input`、`dialog` 等；仅在原生语义不足时添加正确的 `aria-*`，并验证键盘操作、焦点及加载/错误状态。
- 异步操作必须呈现进行中、成功和失败状态；防止重复提交，并清理不再需要的计时器或轮询。

React 原型遵循：

- 使用函数组件和 Hooks；组件名使用 PascalCase。
- 组件保持单一职责。页面级组合放在 `variants/`，通用业务组件放在 `components/`，无业务含义的基础控件放在 `components/ui/`。
- Props 和状态类型显式定义；禁止用 `any` 绕过边界。派生数据优先在渲染期间计算，昂贵计算才使用 `useMemo`。
- 副作用只放在 `useEffect` 或事件处理器中，并提供必要的清理函数；不要用 Effect 同步本可直接派生的状态。

## 3. 命名规范

### 3.1 Python

- 文件、模块、函数、方法、局部变量：`snake_case`。
- 类和异常：`PascalCase`，业务异常以 `Error` 结尾。
- 模块级常量：`UPPER_SNAKE_CASE`。
- 布尔值使用能读成判断的问题式命名，如 `is_ready`、`has_history`、`should_retry`。
- 内部实现可使用单下划线前缀；不要用双下划线制造不必要的名称改写。

### 3.2 JavaScript / TypeScript / React

- 变量、函数、Hook：`camelCase`；自定义 Hook 以 `use` 开头。
- React 组件、类型和接口：`PascalCase`。
- 真正常量且不会重新绑定的配置值可用 `UPPER_SNAKE_CASE`；普通 `const` 变量仍用 `camelCase`。
- 事件处理函数使用 `handleXxx`，传给子组件的回调属性使用 `onXxx`。
- 布尔值使用 `is`、`has`、`can`、`should` 前缀；集合使用复数名。
- DOM 元素 `id` 和 `data-*` 使用 `kebab-case`；CSS 类使用 `kebab-case`，同一组件的子元素可采用 `block__element`，状态使用 `.is-active` 或既有的 `.active`。

### 3.3 名称质量

- 使用项目领域词汇：`project`、`generation`、`visual_item`、`adoption`、`image_job`。不要用 `data`、`info`、`manager`、`helper` 等含义模糊的名称代替真实概念。
- 名称应说明单位或格式，例如 `latency_ms`、`created_at_iso`；不要依赖注释补救含糊命名。
- API JSON 字段沿用现有 `snake_case`，前后端不得为同一字段各造一种拼写。

## 4. 文件组织

### 4.1 Python

一个模块只承担一个主要职责。建议的文件顺序为：

1. 模块文档字符串；
2. `from __future__`；
3. 导入；
4. 模块常量；
5. 公共类型和异常；
6. 公共类/函数；
7. 私有辅助函数；
8. 启动入口。

测试文件放在 `tests/`，命名为 `test_<subject>.py`；测试类命名为 `<Subject>Tests`，测试方法命名为 `test_<behavior>_<condition>`。夹具必须脱敏，文件和数据库使用临时目录。

### 4.2 正式前端

- `static/index.html`：语义结构和静态文案。
- `static/app.js`：真实 API、状态、事件和渲染。
- `static/styles.css`：正式设计系统和响应式样式。
- `static/ui-prototype.*`：仅设计参考，不接真实数据。

当 `static/app.js` 的某个职责已经可以独立命名、测试且持续增长时，先建立变更卡评估 ES modules 和静态托管兼容性，不直接把文件随意拆散。

### 4.3 React 原型

- `frontend/src/components/ui/`：无业务含义的基础控件。
- `frontend/src/components/`：可复用的产品组件。
- `frontend/src/variants/`：页面级方案和布局。
- `frontend/src/lib/`：纯工具；`data.ts`：仅原型假数据。
- 文件名使用 `kebab-case.tsx`；每个文件通常导出一个主要组件。类型贴近使用处，只有被多个模块共享时才抽到独立文件。

配置、提示词和代码分开保存。提示词放在 `config/` 并有版本标识；秘密按第 8 节的注入规则管理，不得进入源码、注释、日志、测试或文档。

## 5. 导入顺序

### 5.1 Python

分组之间空一行，每组内按模块名排序：

1. `from __future__ import ...`；
2. Python 标准库；
3. 第三方库；
4. 本项目绝对或相对导入。

禁止 `from module import *`。只有为解决明确循环依赖或可选依赖时才在函数内导入，并用注释说明原因。

### 5.2 JavaScript / TypeScript

分组之间空一行，每组保持稳定顺序：

1. React 和第三方包；
2. 本项目组件和模块；
3. 样式和其他副作用导入。

类型导入使用 `type`，并与同来源的值导入相邻，不单独分组。

优先使用明确的命名导入，避免不必要的默认导出和深层跨目录引用。当前项目没有路径别名，不得在未配置 TypeScript、Vite 和测试解析器的情况下自行使用 `@/`。

## 6. 格式与语言规则

### 6.1 通用

- 文件使用 UTF-8，默认 LF；Windows 批处理文件按其已验证的 CRLF 要求维护。
- 文件末尾保留一个换行，不留行尾空格。
- 文案和错误消息面向当前中文用户时使用简体中文；代码标识符使用英文。
- 不在同一次业务变更中批量重排、重命名或格式化无关代码。

### 6.2 Python

- 使用 4 个空格缩进和类型标注；公共边界、复杂容器和可能为空的值必须标清类型。
- 优先使用 `pathlib.Path`、上下文管理器和明确异常；禁止裸 `except:`，不要静默吞掉异常。
- 行长度以可读性为准，目标不超过 100 个字符；长参数和容器使用尾随逗号分行。
- 数据库写入应使用参数绑定和明确事务，不拼接不可信 SQL。

### 6.3 JavaScript / TypeScript

- 使用 `const`，仅在需要重新绑定时使用 `let`，禁止 `var`。
- 优先 `async`/`await`，请求失败必须进入统一错误路径。
- 使用严格相等 `===`/`!==`、可选链和空值合并；不要用隐式真假判断区分合法的 `0`、空字符串和 `null`。
- 正式 `static/app.js` 延续当前双引号和分号风格；React 原型延续当前双引号、无分号风格。除非引入并统一配置格式化工具，不跨文件强行统一。
- TypeScript 保持 `strict` 通过；禁止无说明的非空断言和类型强制转换。

### 6.4 CSS

- 优先复用颜色、间距和组件类；同一视觉值重复出现三次以上时评估提取 CSS 自定义属性。
- 选择器保持低特异性，避免 `!important`；若为覆盖第三方样式不得不用，必须注释原因和删除条件。
- 动画尊重 `prefers-reduced-motion`；图片和布局设置明确尺寸，避免内容加载造成大幅跳动。
- 正式 UI 每次修改都检查 `1280x720` 和 `390x844`，不得产生横向溢出。

## 7. 注释与文档

### 7.1 什么时候写注释

复杂逻辑、业务硬约束、兼容层、安全边界、重试/并发策略和不直观的折中必须有注释。注释重点解释“为什么这样做”“违反后会怎样”，不要逐行复述代码。

```js
// 图片状态以服务端为准，避免刷新后把内存中的旧状态覆盖回数据库。
await refreshImageStatus(itemId)
```

以下注释应删除或改写：

```js
// 设置 loading 为 true
state.loading = true
```

- 临时方案写明原因、影响范围和删除条件；如果已有 issue/spec，附仓库内相对路径。
- 代码变化后同步更新注释。失真的注释比没有注释更危险。
- 不在注释、示例或异常消息中放令牌、Cookie、真实提示词、用户上传内容或其他敏感数据。

### 7.2 JavaScript / TypeScript 的 JSDoc

对公共函数、跨模块导出、API 边界和参数/返回值不直观的复杂函数使用 JSDoc。这里的“公共函数”指被其他文件调用、挂载到全局对象或负责 HTTP 请求/响应转换的函数；`static/app.js` 中仅供同一闭包使用的普通函数视为内部函数。简单的局部点击处理器或一眼可懂的纯函数不要求机械补注释。

```js
/**
 * 读取项目历史，并保留服务端返回的“旧定位”标记。
 *
 * @param {number} projectId - 数据库项目 ID。
 * @returns {Promise<object>} 可直接交给历史渲染器的数据。
 * @throws {Error} API 返回失败状态或响应无法解析时抛出。
 */
async function loadHistory(projectId) {
  // ...
}
```

TypeScript 已能表达的类型不必在 JSDoc 中重复；JSDoc 应补充语义、约束、副作用和错误条件。

### 7.3 Python 文档字符串

模块、公共类、公共函数、公共方法，以及边界不直观的私有函数使用简洁的三引号文档字符串。类型信息由类型标注表达，文档字符串说明行为、业务约束、副作用和异常。

```python
def reserve_generation(project_id: int, kind: str) -> dict[str, object]:
    """预留下一批生成记录；达到该类型的批次上限时拒绝创建。"""
```

## 8. API、数据与安全

- JSON API 沿用现有响应包络：成功响应包含 `{"success": true, ...}`，失败响应包含 `{"success": false, "error": "可操作的中文说明"}` 并使用合适的 HTTP 状态码。内部异常不得把堆栈、路径或秘密返回浏览器。
- 外部输入按用途在边界校验类型、长度和枚举；涉及文件访问时额外校验文件名，并确认解析后的目标仍在预期目录。
- `image_prompt` 仅供服务端使用，不得进入浏览器响应。
- 修改 API 字段、SQLite 表、生成 schema 或图片状态机时，必须同步直接调用方、测试、兼容性分析和回滚说明。若变更无需迁移或专门回滚，明确写出理由。
- 日志只记录排错所需的标识、状态、耗时和脱敏错误类别；不得记录令牌、Cookie、完整模型响应或用户文件内容。
- 秘密通过进程环境变量或受控秘密存储注入；本地开发可使用被 Git 忽略的 `chat2api/.env`，不得写进源码或已提交配置。

## 9. 测试与提交前检查

行为修改必须有与风险相称的定向测试：

- 仓储和状态机：临时 SQLite 单元测试。
- API：成功、输入错误、资源不存在和依赖不可用路径。
- AI 结构/提示词：固定脱敏样例和结构校验；未发真实请求时明确标记“未验证”。
- 前端：关键步骤、项目抽屉、真实历史、控制台错误、键盘操作和响应式溢出。

当前最小检查：

```powershell
$env:PYTHONPATH = "D:\code\ai_creative_studio\src"
python -m unittest discover -s tests -v
node --check static\app.js
python -m compileall -q src chat2api
git diff --check
```

修改 `frontend/` 时另执行：

```powershell
Set-Location frontend
npm run build
```

仓库目前没有统一启用 ESLint、Prettier 或 Ruff，因此不得把“本地编辑器无警告”表述为 lint 已通过。若引入这些工具，应单独提交配置、固定版本、排除目录和全库基线，避免与功能改动混在一起。

## 10. 代码审查清单

提交前确认：

- [ ] 改动位于真实入口，且没有把原型假数据接入正式应用。
- [ ] 命名使用项目领域词汇，函数和文件职责清楚。
- [ ] 复杂逻辑解释了原因、约束和失败后果。
- [ ] 用户、数据库和 AI 内容在进入 HTML、SQL、路径或日志前已正确处理。
- [ ] 浏览器临时状态没有取代 SQLite 事实状态。
- [ ] API/数据库/生成结构变化有兼容、测试和回滚方案。
- [ ] 没有提交 `.env`、令牌、Cookie、运行数据库、生成图片或真实上传；确需提交的最小测试夹具已脱敏、用途明确并经过审查。
- [ ] 已执行影响范围内的检查，并如实记录未验证项。
- [ ] 用户可见行为变化已更新 `CHANGELOG.md`；任务收尾已更新 `progress.md`。
