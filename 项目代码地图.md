# 项目代码地图

## 当前状态

这是一个独立的 Windows 本地网页应用。网页服务和 AI 网关默认分别监听 `127.0.0.1:8775`、`127.0.0.1:8780`。代码已有应用内登录、用户表、管理员角色和项目所有权隔离；当前仍缺少自动备份、恢复演练、正式内网部署和公网安全配置，因此暂不适合直接作为多人生产服务。当前 AI 架构和迁移规则以 [`docs/ai-rebuild-master-plan.md`](docs/ai-rebuild-master-plan.md) 为准，`progress.md` 只记录历史事件。

## 目录

```text
D:\code\ai_creative_studio
├─ AGENTS.md                         智能体和开发规则
├─ CODE_STYLE.md                     技术栈、命名、注释、文件组织和验证规范
├─ README.md                         使用说明和当前功能边界
├─ progress.md                       当前进度入口
├─ CHANGELOG.md                      用户可见变更记录
├─ 项目代码地图.md                   本文件，入口和职责索引
├─ 启动AI创意工作台.bat              双击启动本地控制台
├─ launcher.py                        启动控制台、服务进程、登录配置和项目专用代理中转
├─ proxy_workbench.py                 独立网络代理工作台入口
├─ src/creative_studio/
│  ├─ app.py                          HTTP 服务、API 路由、静态文件分发
│  ├─ repository.py                   SQLite 表结构、用户/项目、生成历史、采用关系
│  ├─ auth.py                         密码哈希、会话、CSRF、账号管理和权限上下文
│  ├─ generation_service.py            生成编排、保留/完成/失败、图片派发和轮播继续
│  ├─ narrative.py                     NarrativeResult.v1 叙事生成 Module、校验、repair 和公开 DTO
│  ├─ generation_models.py             生成请求、快照、上下文和稳定错误类型
│  ├─ ai_creative.py                  提示词构建、类型分流、JSON 校验和兼容 AI 函数
│  ├─ prompting.py                    模板变量替换和 prompt 编译辅助
│  ├─ model_client.py                 Chat2API 文字模型 HTTP 适配器
│  ├─ carousel.py                     轮播数量和逐帧结构归一化/校验
│  ├─ image_jobs.py                   图片网关任务、下载、状态和本地文件保存
│  ├─ public_projection.py            浏览器可见结果的 deny-by-default 白名单投影
│  ├─ prompt_registry.py              PromptSpec、GenerationPolicy 和启动校验
│  ├─ contracts.py                    显式 schema/validator/projector/caller allowlist
│  ├─ model_ports.py                  Text/Image Port 与 deterministic fake
│  ├─ projection_scrub.py             旧历史公开投影的只读扫描和临时库重建工具
│  ├─ ai_provider.py                  OpenAI 兼容地址辅助
│  └─ ai_service_settings.py          AI 配置数据结构和地址规范化
├─ static/
│  ├─ index.html                      正式三步工作台结构
│  ├─ app.js                          步骤流、项目编辑、生成、历史、采用和图片轮询
│  ├─ styles.css                      炭灰×青绿正式页面和响应式样式
│  ├─ ui-prototype.*                  已选 B 方案的设计参考，不连接真实 API
│  └─ tag-flow-prototype.*            标签折叠流程 A/B/C 视觉原型，仅内存交互
├─ config/
│  ├─ prompts/registry.json           Phase 1 prompt inventory 和 production registry
│  ├─ evals/                          脱敏 deterministic evaluation fixtures
│  ├─ ai_creative_prompt_v5.txt       叙事类提示词
│  ├─ ai_visual_creative_prompt_v2.txt 展示类提示词
│  ├─ ai_visual_static_creative_prompt_v1.txt 静态展示类目标提示词（当前尚未接入）
│  ├─ ai_visual_carousel_prompt_v1.txt 轮播展示类规划提示词
│  ├─ ai_visual_first_frame_prompt_v1.txt 历史/未接线的独立首帧提示词
│  ├─ ai_visual_follow_up_prompt_v1.txt 历史/未接线的后续画面提示词
│  ├─ ai_creative_game_info_v2.json   游戏背景摘要
│  └─ creative_tag_options.json       叙事 6 组、展示 11 组标签和值域配置
├─ chat2api/                          独立 ChatGPT Web 网关副本
├─ data/                               运行数据库、图片和参考文件
├─ tests/test_repository.py           当前仓储和数据状态机定向测试
└─ docs/
   ├─ operations.md                  启停、备份、恢复和故障处理
   ├─ 变更卡模板.md                   每次改动的最小记录格式
   ├─ ai-rebuild-master-plan.md      AI 能力当前事实、问题台账、目标架构和迁移总纲
   ├─ ai/README.md                   AI 文档入口和后续 Agent 最短执行路径
   ├─ adr/0001-public-result-projection.md 公开/私有结果边界决策
   ├─ research/                       权威工程实践和安全资料
   └─ ...
```

## 真实调用链

### 页面到文字方案

```text
static/index.html
  ↓ static/app.js
POST /api/projects/{id}/generate
  ↓ src/creative_studio/app.py
StudioApplication.generate
  ↓ CreativeGenerationService.generate
  ↓ _generate_with_model_client（正常配置路径）
NarrativeGeneration（叙事）或 ai_creative 视觉分支
  ↓ HttpModelClient
  ↓ HTTP POST /v1/chat/completions
chat2api/routes_chat.py
  ↓ 返回结构化 JSON
repository.complete_*_generation
  ↓
GET /api/projects/{id}/history
  ↓ static/app.js 渲染批次
```

### 展示类图片

```text
展示类文字批次成功
  ↓ repository.complete_visual_generation
visual_items(image_prompt, image_status=queued)
  ↓ ImageJobRunner.enqueue
POST 127.0.0.1:8780/v1/images/jobs
  ↓
轮询 GET /v1/images/jobs/{job_id}
  ↓ 成功后下载同源图片
data/images/{generation_id}/{item_id}/attempt-{n}.<ext>
  ↓
GET /api/visual-items/{id}/image
```

### 展示类轮播会话

```text
POST /api/projects/{id}/generate
  ↓ 一次共享文字会话输出 3 套方案和各自完整路线
每套 visual_item
  ↓ 直接使用共享响应中的 first_frame 指令入队图片任务
ImageJobRunner.enqueue
  ↓ 首帧完成后
POST /api/visual-items/{id}/continue
  ↓ 复用该方案会话，使用 generation_service.py 硬编码指令按 2、3……顺序生成后续画面
```

提示词文件中仍有“每套方案重新建立文字会话生成首帧”的旧承诺，但当前生产代码没有这三次文字调用；这是必须在 Phase 3/4 通过 ADR、prompt 和测试一起收口的已知漂移，不应继续按旧文案扩展功能。

## API 表

| 地址 | 用途 |
|---|---|
| `GET /api/health` | 本地健康检查 |
| `GET /api/auth/status`、`POST /api/auth/login`、`POST /api/auth/logout` | 登录状态和会话管理 |
| `GET /api/auth/me`、`POST /api/auth/password` | 当前账号和修改密码 |
| `GET/POST /api/admin/users` | 管理员读取/创建普通账号 |
| `GET /api/tag-options` | 读取叙事类和展示类标签配置 |
| `GET /api/projects` | 项目列表和搜索 |
| `POST /api/projects` | 新建项目 |
| `GET /api/projects/{id}` | 读取项目 |
| `PUT /api/projects/{id}` | 保存项目输入 |
| `DELETE /api/projects/{id}` | 删除项目及其关联数据 |
| `POST /api/projects/{id}/files` | 保存参考文件 |
| `POST /api/projects/{id}/generate` | 生成一批叙事或展示方案 |
| `GET /api/projects/{id}/history` | 读取当前和旧定位历史 |
| `POST /api/projects/{id}/adopt` | 采用一条方案 |
| `GET /api/visual-items/{id}/status` | 读取图片状态 |
| `POST /api/visual-items/{id}/retry` | 重试失败图片 |
| `GET /api/visual-items/{id}/image` | 读取已生成图片 |

## 数据表

- `users`：账号、角色、密码摘要、启用状态和审计相关字段。
- `projects`：项目输入、所有者和更新时间；普通用户只能访问自己的项目，管理员可访问全部项目。
- `project_files`：参考文件名和本地存储名。
- `generations`：叙事/展示类型、指纹、批次、原始结构化结果和会话标识。
- `visual_items`：展示方案正文、服务端图片提示词、图片任务状态和路径。
- `display_frames`：方案逐帧路线、实际内容、图片状态和隐藏图片指令。
- `adoptions`：每个项目当前采用的一条方案。

## 修改导航

| 想改什么 | 首选文件 | 同步检查 |
|---|---|---|
| 页面布局和文案 | `static/index.html`、`static/styles.css` | `static/app.js`、页面静态冒烟 |
| 项目输入和保存 | `static/app.js`、`repository.py` | `app.py` API、仓储测试 |
| 展示类提示词 | `config/ai_visual_creative_prompt_v2.txt` | `ai_creative.py`、AI 评估样例 |
| 叙事类提示词 | `config/ai_creative_prompt_v5.txt` | 叙事结构校验和回归样例 |
| AI 返回结构 | `ai_creative.py` | 对应类型的生成与校验测试 |
| 图片任务 | `image_jobs.py`、`repository.py` | 网关状态、失败重试、文件完整性 |
| 公开结果字段 | `public_projection.py` | history/status/adopt/project 路由、递归私有字段测试 |
| 旧历史投影检查 | `projection_scrub.py` | 只读 dry-run、临时库 backup/apply/恢复 |
| 数据库字段 | `repository.py` | 版本化迁移、备份和恢复检查 |
| 启动端口和环境 | `启动AI创意工作台.bat`、`chat2api/config.py` | 端口隔离、健康检查 |
| 出站代理配置 | `proxy_workbench.py`、`launcher.py`、`chat2api/config.py` | `PROXY_URL` 脱敏、项目专用 Mihomo 中转、出口 IP 测试、重启生效 |

## 已知缺口

这些是未来“给公司同事使用”前的独立变更，不应在没有变更卡和用户确认时顺手加入：正式内网绑定与 HTTPS、反向代理和外部限流、自动备份、恢复演练、AI 质量评估集、依赖锁定、服务器自动启动和更完整的审计报表。认证、`user_id` 项目归属和管理员角色已经存在，后续只能做有范围、有回滚的增强。
