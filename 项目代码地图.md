# 项目代码地图

## 当前状态

这是一个独立的 Windows 本地网页应用。网页服务和 AI 网关默认分别监听 `127.0.0.1:8775`、`127.0.0.1:8780`。应用已有登录、管理员角色、项目所有权隔离、备份/恢复工具和 AI v2 正式页面接线，但尚未达到正式多人或公网部署标准。

正式 AI 业务代码只位于 `src/creative_studio/ai_v2/`。根包旧 AI 实现、旧 Prompt 和旧 v1 评测资产已删除；已有运行库中的旧表只作为历史数据保留，不由当前仓储创建、读取或迁移。历史架构见 ADR、handoff 和 `docs/ai-rebuild-master-plan.md`，当前评测边界见 `docs/ai/quality-evaluation.md`。

## 目录

```text
D:\code\ai_creative_studio
├─ AGENTS.md                         智能体和开发规则
├─ CODE_STYLE.md                     技术边界、命名和验证规范
├─ README.md                         使用说明和当前功能边界
├─ progress.md                       追加式历史时间线
├─ CHANGELOG.md                      用户可见变更
├─ launcher.py                       启动控制台、服务和代理中转
├─ proxy_workbench.py                网络代理工作台
├─ src/creative_studio/
│  ├─ app.py                         HTTP、认证接线、项目 API、v2 API 和静态分发
│  ├─ repository.py                  用户、会话、审计、项目和项目文件 SQLite 仓储
│  ├─ project_projection.py          项目详情/列表的公开白名单投影
│  ├─ auth.py / auth_cli.py          认证、账号管理和管理员初始化
│  ├─ backup.py                      数据库、图片和上传的备份/恢复
│  └─ ai_v2/
│     ├─ application.py              v2 用例门面和显式注入的 candidate model
│     ├─ http_api.py                 `/api/v2` 路由分发
│     ├─ input_contract.py           三字段输入规范化
│     ├─ schema.py                   v2 输出 schema 校验
│     ├─ prompt_registry.py          v2 Prompt registry 与模板编译
│     ├─ narrative.py                叙事文字用例
│     ├─ static_visual.py            静态展示文字用例
│     ├─ carousel_visual.py          轮播文字用例
│     ├─ image_worker.py             按需图片执行与对账
│     ├─ session.py                  图片会话策略
│     ├─ store.py / migrations.py    `ai_v2_*` 持久化
│     ├─ projection.py               v2 公开 DTO
│     ├─ boundary.py                 旧 AI 引用/路由/表边界守卫
│     ├─ release_gate.py             deterministic contract 门禁
│     ├─ production_gate.py          独立 production readiness contract 门禁
│     ├─ fakes.py                    测试模型
│     └─ adapters/                   `chat2api` 文字/图片适配器
├─ static/
│  ├─ index.html / app.js / styles.css 正式根页面
│  ├─ ai-v2/                         独立诊断页面
│  └─ *prototype*                    设计参考，不接正式 API
├─ config/
│  ├─ creative_tag_options.json      页面标签和值域配置
│  ├─ ai_v2/prompts/                 v2 candidate Prompt 与 registry
│  ├─ ai_v2/schemas/                 v2 JSON schema
│  └─ evals/ai_v2/                   v2 脱敏 case 与 contract 报告
├─ chat2api/                          本项目保留的 ChatGPT Web 网关
├─ data/                              真实运行数据库、图片和上传，禁止提交
├─ tests/                             中立基础设施与 AI v2 测试
└─ docs/                              运维、ADR、研究、规格、计划和 handoff
```

## 真实调用链

### 项目与页面

```text
static/index.html + static/app.js
  -> /api/auth/*                       登录、CSRF、账号管理
  -> /api/projects*                    项目 CRUD、上传
  -> StudioRepository                  中立 SQLite 表
  -> ProjectProjection                 浏览器项目 DTO
```

项目详情不再读取旧 `adoptions`。页面加载项目后，通过 `/api/v2/projects/{id}/adoption` 单独读取当前 v2 采用状态。

### AI v2 文字

```text
POST /api/v2/projects/{id}/generate
  -> AiV2HttpApi
  -> AiV2Application
  -> NarrativeTextUseCase / StaticTextUseCase / CarouselTextUseCase
  -> AiV2PromptRegistry + v2 schema
  -> TextGatewayAdapter（显式 live）；测试可显式注入 CandidateTextModel
  -> SqliteAiV2Store
  -> v2 公开 DTO
```

每批文字一个 session，一次模型调用。业务输入只接受 `task_description`、`aspect_ratio`、`creative_tags`。

### AI v2 图片

```text
POST /api/v2/schemes/{scheme_id}/image
  -> AiV2Application + ImageWorker
  -> 每方案一个 ImageSessionCursor
  -> 每次点击最多推进一张
  -> ImageGatewayAdapter（显式 live）；测试可显式注入 CandidateImageModel
  -> ai_v2_image_attempts / ai_v2_artifacts
  -> GET /api/v2/image-attempts/{attempt_id}
```

重试先对账并复用原图片会话；只有明确终态失败才创建新 attempt。轮播同一方案的全部帧共享一个图片会话。

## API

| 地址 | 用途 |
|---|---|
| `GET /api/health` | 本地健康检查 |
| `/api/auth/*` | 登录、会话、密码和用户管理 |
| `GET /api/tag-options` | 标签配置 |
| `GET/POST /api/projects` | 项目列表、新建和搜索 |
| `GET/PUT/DELETE /api/projects/{id}` | 项目读取、保存和删除 |
| `POST /api/projects/{id}/files` | 保存项目文件 |
| `POST /api/v2/projects/{id}/generate` | 生成一批 v2 文字方案 |
| `GET /api/v2/projects/{id}/history` | 读取 v2 历史 |
| `POST /api/v2/schemes/{id}/image` | 按需推进一张图片 |
| `GET /api/v2/image-attempts/{id}` | 图片 attempt 状态 |
| `GET /api/v2/image-attempts/{id}/image` | 成功图片内容 |
| `POST /api/v2/projects/{id}/adopt` | 采用 v2 方案 |
| `GET /api/v2/projects/{id}/adoption` | 当前 v2 采用状态 |

旧 `/api/projects/{id}/generate`、`/api/visual-items/*` 和旧采用路由均返回 404。

## 数据表

中立仓储创建：

- `users`、`sessions`、`login_attempts`、`audit_logs`
- `projects`、`project_files`

AI v2 存储创建：

- `ai_v2_runs`、`ai_v2_text_sessions`、`ai_v2_schemes`、`ai_v2_frames`
- `ai_v2_image_sessions`、`ai_v2_image_attempts`、`ai_v2_artifacts`、`ai_v2_adoptions`

已有数据库可能仍含旧 `generations`、`visual_items`、`display_frames`、`carousel_operations`、`adoptions`。当前代码不创建、不迁移、不读取或删除这些表；`backup.py` 只为历史备份完整性保留对旧成功图片路径的只读检查。

## 修改导航

| 想改什么 | 首选文件 | 同步检查 |
|---|---|---|
| 页面布局和交互 | `static/index.html`、`static/app.js`、`static/styles.css` | 前端 contract、桌面/移动浏览器 |
| 项目输入、文件和权限 | `repository.py`、`app.py` | repository/auth/app 测试 |
| 项目公开字段 | `project_projection.py` | project projection 与 API 测试 |
| v2 Prompt / schema | `config/ai_v2/`、`ai_v2/prompt_registry.py` | prompt registry、schema、release gate |
| v2 文字用例 | `ai_v2/narrative.py`、`static_visual.py`、`carousel_visual.py` | 对应用例和集成测试 |
| v2 图片状态/重试 | `ai_v2/image_worker.py`、`session.py`、`store.py` | image state/worker/session 测试 |
| v2 公开 DTO | `ai_v2/projection.py` | privacy、projection、integration 测试 |
| 备份/恢复 | `backup.py`、`docs/operations.md` | backup 测试和临时恢复演练 |
| 网关和代理 | `chat2api/`、`launcher.py`、`proxy_workbench.py` | loopback、脱敏和健康检查 |

## 已知缺口

三份 v2 Prompt 仍为 `candidate`、`caller=null`；正式入口未启用 live 时 fail-closed，不连接真实 AI。真实模型质量、生产 caller、正式多人部署、自动备份、HTTPS、外部限流和服务器调度均需独立审批与变更卡。
