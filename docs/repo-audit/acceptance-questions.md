# 新会话验收问题

1. 项目正式网页和 AI 网关的启动入口、监听地址与端口分别是什么？回答需引用当前源码或运维文档。
2. 若要修改 AI v2 的一个生成用例，应先定位哪些真实模块、配置 registry、schema、测试和生产 caller？回答需区分正式入口与设计参考。
3. 当前审计有哪些未完成范围或阻塞？回答需从 STATE/QUEUE 和源证据核对，不把 deterministic 测试或历史 progress 记录当成全部完成。
4. 按项目规则，下一步应执行哪个任务，哪些动作明确不能在本轮顺手做？
5. 运行最小验证命令需要哪些环境前提？哪些真实 AI、图片、部署或公网动作未被本轮验证？
