# 审计记录纠正与 T04 停止点

## 本轮边界

仅修正审计记录、覆盖映射、队列依赖和执行入口；不执行新的 T02、T03 或 T04 核对，不修改业务代码、正式项目文档或 `progress.md`。

## 五处问题处理

1. `T02-002` 主体已有报告和 38 项定向测试证据，恢复为 `done`。`T02-007` 不再作为等待父任务的独立审计任务，标为 `not_applicable`，保留历史来源；F-005 程序修复延期，备份文档声明并入 `T03-001`。
2. `F-005` 继续标记“已证实”，状态改为 `deferred`，明确“发现不等于修复”，不作为文档审计完成条件。
3. `coverage.csv` 已按十列表头整理，含逗号字段按标准 CSV 引号规则保存；所有行经标准解析检查为十列。
4. 覆盖表补充根规则、运维/AI、deployment/release、ADR/research 和受限 superpowers 文档的真实路径组；均标为已清点但未审查，`progress.md` 明确排除。
5. 历史 T01/T02 报告通过勘误保留 `HEAD 6f18b62` 过程；当前 STATE、待执行队列和覆盖记录使用真实 `HEAD be89f8a` 及当前未提交差异摘要。

## 阶段停止点

完成剩余 T02-003 至 T02-006 后，执行独立 T03-001/002/003；再执行全部 T04-001/002。T04 最终交付需包含问题清单、有效约束、未验证范围、初步文档整理映射和后续建议。完成 T04 后停止，不自动执行 T05-T08。

## 当前下一任务

下一会话继续 `T02-003`。最小必读：`repo-audit-kit/RUN_ONE_TASK.md`、`docs/repo-audit/STATE.md`、`docs/repo-audit/QUEUE.csv`、本报告、`docs/repo-audit/reports/T02-002.md`、`docs/repo-audit/findings.csv`、相关任务卡。程序修复和正式文档修订留待后续专项授权。
