# T03-002 部署与发布文档核对

## 任务 ID、目标与范围

核对部署基准、实时部署记录、公网启动手册、多人与发布/回滚 runbook 中的启动、端口、配置、备份、回滚和完成状态；不执行部署、不修改文档或 `progress.md`。

## 基线与实际读取

基线 `HEAD be89f8a`。回查 `launcher.py`、`chat2api/main.py`、`src/creative_studio/app.py`、`backup.py`、T02-003/T02-005 报告、当前 `STATE.md` 和文档内命令/端口。

## 已证实结论

1. `docs/deployment/code-baseline-20260908.md`、`docs/deployment/live-deployment.md`、`public-startup-guide.md` 均明确 8775 网页、8780 网关和 `chat2api\\.env`/数据目录等入口；launcher/chat2api 源码支持默认回环和 8780 隔离。`CHATGPT_IMAGE_JOB_DIR`、`CREATIVE_STUDIO_DATA_DIR` 等路径注入也有实现/定向测试证据。
2. `docs/release/runbook.md` 与 `rollback.md` 的停写、备份、restore smoke、回退代码/registry 不覆盖新数据库、保留日志和未验证项等约束，与备份工具和项目高风险门禁方向一致。F-005 仍使目标目录 containment 不能当作已修复事实。
3. `public-startup-guide.md` 是公网 HTTP 操作手册，设置 `CREATIVE_STUDIO_HOST=0.0.0.0`、`CREATIVE_STUDIO_AI_V2_LIVE=1` 并创建 8775 防火墙规则；文档自身注明无 HTTPS、仅适合受控使用。该操作超出默认回环/高风险门禁，必须作为已授权历史方案而非默认项目规则，不能由静态文档证明已安全验收。
4. 新增 F-008：部署文档内部状态与当前审计证据不一致。`code-baseline` 声称 annotated tag、unittest 200 项和 gate 全部通过；`live-deployment` 同时保留“步骤 1 进行中/尚未上传或切换”与步骤 2 已生成备份/部署包、服务器启动和公网开放记录；当前审计基线为 master `be89f8a`，全量 unittest 199 项 `3 failures/44 errors`。这些记录需要按版本/时间分层，不能合并成当前完成结论。
5. `live-deployment.md` 记录了公网 IP、服务器、数据迁移和用户确认，但本轮未重新访问服务器；文档明确公网/数据库迁移、真实 AI、部署和公网验收仍有限制。此类外部事实作为历史证据保留，不升级为本轮验证。

## 推断与待确认意图

- 部署记录可能跨多个工作树/服务器快照，需 T04-001 结合 launcher、端口和环境变量确认哪个入口是当前有效约束。
- 是否继续保留公网 HTTP 手册、改为仅内部受控使用或另建 HTTPS 变更卡，需后续用户/部署负责人决定；本轮不修改。

## 实际修改与检查

仅新增报告并更新审计台账；未启动服务、连接服务器、写入 `.env`、迁移数据或修改正式文档。执行文档命令/端口检索和本地实现回查。

## 未完成项与下一动作

- `T03-003`：核对 ADR、research 和受限 superpowers 文档。
- F-008 交给 T04-001 复核启动/部署关系；F-005/F-006 继续 deferred。

## 完成条件逐项核对

- 启动/端口/配置/备份/回滚声明：已核对。
- 外部部署和公网事实：保留来源并标记未重新验证。
- 未执行部署或安全配置变更。

## 下一轮最小必读资料与下一动作

读取本报告、T02-003/T02-006、T03-001、队列和 T03-003 任务卡，执行 `T03-003`。
