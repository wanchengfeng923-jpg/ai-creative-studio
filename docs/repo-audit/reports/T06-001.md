# T06-001 当前事实索引与历史记录分层

## 任务 ID、目标与范围

本轮执行 `repo-audit-kit/tasks/T06_docs_and_history.md`。范围是 T03/T04 已确认的 registry/readiness/production-cutover、部署基准/实时记录和 2026-09-06 handoff/审批文档。目标是让新会话能区分当前仓库事实、历史快照和待业务确认项；不修改业务代码、Prompt、registry、部署配置或 `progress.md`。

## 开始和结束基线 / 差异

- 开始和结束源码基线：`HEAD be89f8a5ec7d8103c83ad86bf49dbb95971ff70a`，当前分支 `master`。
- 已有差异保留：`progress.md` 已修改；`docs/repo-audit/`、`repo-audit-kit/` 及 zip 为审计/交接材料。
- 本轮新增/修改：`docs/current-state.md`、`AGENTS.md`、`README.md`、`docs/project-rules/operations.md`、`docs/ai-rebuild-master-plan.md`、`docs/project-rules/项目代码地图.md`、两份部署记录、Prompt 审批包、三份 2026-09-06 handoff，以及本报告、覆盖记录、STATE/QUEUE。没有改业务代码、registry、Prompt 正文或 `progress.md`。

## 实际读取与核对的关键资料

- `AGENTS.md`、`RUN_ONE_TASK.md`、`HANDOFF_FORMAT.md`、T03-001~003、T04-FINAL-20260908、T05-001。
- `config/ai_v2/prompts/registry.json`、三份模板、`src/creative_studio/ai_v2/prompt_registry.py`；项目 `.venv` 直接加载 registry。
- `docs/project-rules/operations.md`、`docs/project-rules/项目代码地图.md`、`docs/ai-rebuild-master-plan.md`、`docs/ai/ai-v2-prompt-approval.md`。
- `docs/deployment/code-baseline-20260908.md`、`docs/deployment/live-deployment.md`。
- `2026-09-06-ai-v2-next-session-handoff.md`、`readiness-baseline.md`、`production-cutover-handoff.md`，以及相关 image evidence/current-state/cutover follow-up 的状态段落。
- Git ref：当前 `master`=`be89f8a...`；`production-baseline-20260908` 解析到 `b28c562...`；`codex/tag-accordion-prototype`=`ef83cd8...`。

## 已证实结论及证据

1. 当前 registry 文件声明三项 Prompt 为 `production` 并绑定 caller；narrative 模板 hash 一致，static/carousel 实际 hash 分别为 `fb2b0989...`、`a956e80f...`，与 registry 不一致。`AiV2PromptRegistry` 在当前文件上抛出 `AiV2PromptRegistryError: v2 prompt template hash mismatch`。F-006 仍是运行就绪阻断，不能把 lifecycle 字段当作可加载或真实质量通过。
2. `AGENTS.md` 已由 T05 固定 registry 声明与运行就绪、直接 app fail-closed 与 launcher live 注入的边界。本轮把这条规则接到当前事实索引，不扩大授权。
3. 原代码地图/运维文档的 candidate/null 状态与当前 registry 不一致；部署基准的 tag/测试结论和实时部署文档的步骤记录来自不同时间点；handoff/审批包同时存在 candidate 和 production 结论。它们分别作为历史来源保留，不在本轮选择业务批准方。
4. 当前索引明确：工作树是 `master` HEAD `be89f8a`；tag 和部署包属于其他 ref；外部服务器、公网、真实 AI/图片、移动浏览器和正式发布仍未由本轮验证。

## 实际修改与原因

- 新增 `docs/current-state.md`：集中记录规则/资料入口、registry hash 与可加载性、Git/deployment ref、历史 handoff 分层、待确认项、维护触发条件和 T07/T08 交接。
- 更新 `AGENTS.md`、`README.md`：让新会话先找到当前事实索引，同时保留 `progress.md` 为历史时间线。
- 更新 `docs/project-rules/operations.md`、`docs/project-rules/项目代码地图.md`：把已证实的 registry/hash 状态写清，去除 candidate/null 的当前事实误导，并链接索引。
- 更新 `docs/ai-rebuild-master-plan.md`：保留其 AI 重构实施权威角色，但要求测试/registry/审批/部署状态按当前证据复核。
- 更新部署基准、实时部署、Prompt 审批包及三份 2026-09-06 handoff 顶部标识：这些文件是历史快照或多时点记录，先读当前索引再解释。
- 更新 `QUEUE.csv`、`STATE.md` 和 `coverage.csv`；原 `findings.csv` 的 F-007/F-008/F-009 保留原始开放记录，避免把“索引歧义缓解”误写成业务批准或外部部署已完成。

## 采用、调整和未采用建议

| 建议 | 处置 | 理由 |
|---|---|---|
| 建立短的当前事实入口，分开规则、当前状态、历史和任务记录 | 采用 | 符合指南的单一维护位置和渐进披露；不复制完整审计台账 |
| 直接把 registry `production` 或 cutover handoff 当作当前可运行/已批准 | 不采用 | F-006 hash mismatch、ref 不同且缺少当前独立批准/受控验证 |
| 覆盖或删除旧 handoff、部署记录、审批包 | 不采用 | 它们是可追溯历史来源，删除会丢失冲突证据 |
| 修复 Prompt hash、程序缺陷、部署和真实验证 | 不采用/延期 | 分别属于 F-006、F-003/F-004/F-005/F-010 或 T07/T08/独立授权范围 |
| 处理 `progress.md` | 不采用 | 用户明确排除；保留现状和既有读取安排 |

## 执行的检查、结果及日志位置

- `Get-FileHash`：附件指南与 `repo-audit-kit/reference/` 副本一致；三份模板 hash 与 registry 的差异复现。
- 项目 `.venv` 直接构造 `AiV2PromptRegistry`：按预期失败并输出 `v2 prompt template hash mismatch`；没有修改配置来让检查通过。
- `git show-ref` / `git show`：确认当前 `master`、production tag 和 `codex/tag-accordion-prototype` 为不同 ref。
- `git diff --check`：通过（退出码 0）；Git 仅提示工作树文件的 CRLF 将在后续 Git 操作中转为 LF，不是校验失败。
- Python `csv.DictReader`：`QUEUE.csv`、`coverage.csv`、`findings.csv` 均可解析；结构化状态为 T05 `done`、T06 `done`、T07/T08 `todo`。
- 相关文档内部 Markdown 链接检查：通过；没有发现本轮新增断链。
- `git diff --name-only`：仅包含本轮文档入口/索引与既有 `progress.md` 差异；未出现业务代码、registry 或 Prompt 正文。
- 未运行全量 unittest、release gate、真实 AI/图片、浏览器、服务器、公网和部署写入检查；这些限制写入当前索引和 STATE。

## 未完成项与后续任务

- F-006：修复 registry/template hash 并重新做配置/定向验证，需单独授权。
- F-009：当前 master 的最终 Prompt 批准、批准人/时间和 hash 对应关系仍待业务证据；本轮只完成历史分层。
- F-008/F-010：外部服务器 ref、live 启用、审批、备份/回滚和受控 smoke 仍未验证。
- F-003/F-004/F-005：程序接口、版本来源和恢复边界缺陷保持 deferred。
- 下一任务 `T07-001`：由新会话按任务卡做整理结果、引用和加载边界的定向验证；随后 `T08-001` 必须由再下一新会话独立验收。

## 对其他任务的影响

T07 应以 `docs/current-state.md`、`AGENTS.md`、本报告、`STATE.md`、`QUEUE.csv` 为最小入口，检查链接和语义是否一致；不要把历史 handoff 的阶段测试数字当作当前测试结果。T08 需要新会话确认能独立发现正式规则入口、当前事实、资料分类、维护触发条件和剩余限制。

## 完成条件逐项核对

- [x] 当前事实、历史快照、待确认状态已建立分层入口。
- [x] 已修改关联文档并更新引用；旧 handoff、部署记录和审批包原文保留。
- [x] 已保留有效规则、例外、高风险授权和 fail-closed 语义；未新增无依据强制要求。
- [x] registry、Prompt、业务代码、部署配置和 `progress.md` 未被修改。
- [x] QUEUE、STATE、coverage 和本报告已保存；下一入口为 T07-001。
- [ ] 新会话规则加载、链接全量和跨会话独立接手验证：交给 T07/T08。

## 下一轮最小必读资料与下一动作

读取 `AGENTS.md`、`docs/current-state.md`、本报告、`STATE.md`、`QUEUE.csv`、T05-001 和 T04-FINAL；执行 `repo-audit-kit/tasks/T07_validation.md`。不要重复重写历史 handoff 或处理 `progress.md`。
