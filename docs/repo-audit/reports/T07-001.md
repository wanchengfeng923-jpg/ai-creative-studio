# T07-001 整理结果与引用定向验证

## 任务 ID、目标与范围

本轮执行 `repo-audit-kit/tasks/T07_validation.md`。范围是核对 T05-001/T06-001 形成的规则入口、当前事实索引、历史分层引用、审计台账和加载边界。仅做文档/规则验证与交接记录；不修复业务代码、Prompt、registry、部署配置，不处理 `progress.md`，也不执行 T08 独立验收。

## 开始和结束基线 / 差异

- 开始和结束源码基线：`HEAD be89f8a5ec7d8103c83ad86bf49dbb95971ff70a`，分支 `master`，跟踪 `origin/master`。
- 工作树已有 T05/T06 文档改动、`progress.md` 用户改动和未跟踪审计/任务包材料；本轮没有覆盖或删除这些差异。
- 本轮实际新增/修改：本报告、`docs/repo-audit/QUEUE.csv`、`STATE.md`、`coverage.csv` 和 `docs/current-state.md` 的当前交接段落。没有修改业务代码、Prompt、registry、部署配置或 `progress.md`。

## 实际读取与核对的关键资料

- `repo-audit-kit/RUN_ONE_TASK.md`、`HANDOFF_FORMAT.md`、`tasks/T07_validation.md`、`docs/repo-audit/ACTIVE_RUN.md`。
- `docs/repo-audit/STATE.md`、`QUEUE.csv`、`coverage.csv`、`findings.csv`、`reports/T05-001.md`、`reports/T06-001.md`、`reports/T04-FINAL-20260908.md`。
- `AGENTS.md`、`README.md`、`docs/current-state.md`、`docs/project-rules/operations.md`、`docs/ai-rebuild-master-plan.md`、`docs/project-rules/项目代码地图.md`。
- 部署/审批历史快照：`docs/deployment/code-baseline-20260908.md`、`docs/deployment/live-deployment.md`、`docs/ai/ai-v2-prompt-approval.md` 和 2026-09-06 handoff 三份文件。
- 实现证据：`launcher.py`、`src/creative_studio/app.py`、`src/creative_studio/ai_v2/prompt_registry.py`、`production_gate.py`、`config/ai_v2/prompts/registry.json` 及三份模板。

## 已证实结论及证据

1. `QUEUE.csv` 结构化统计为 T01 `done=1`、T02 `done=6/not_applicable=1`、T03 `done=3`、T04 `done=2`、T05 `done=1`、T06 `done=1`、T07 `done=1`、T08 `todo=1`；17 行队列依赖全部可解析且无缺失引用。
2. 附件指南与 `repo-audit-kit/reference/` 副本 SHA-256 一致；本轮继续以仓库事实和有效规则为准，没有把指南示例升级成项目指令。
3. 12 个本轮相关正式文档的本地 Markdown 链接检查无断链；`AGENTS.md`、`README.md`、`docs/current-state.md`、运维/部署/审批/handoff 文件引用的当前事实入口和目标路径均存在。
4. 入口和命令与源码一致：网页默认 `127.0.0.1:8775`，网关默认 `127.0.0.1:8780`；`launcher.py` 注入 `PYTHONPATH`、端口和 `CREATIVE_STUDIO_AI_V2_LIVE=1`；直接 `create_application()` 默认仍以未启用 live 的 `ai_not_enabled` fail-closed。`prompt_registry.py` 默认从 `config/ai_v2/prompts/registry.json` 加载，并将模板路径限制在 registry 目录内。
5. 当前 registry/template hash 结果与索引一致：narrative 一致；static 声明 `c820c9...51139b`、实际 `fb2b09...d64e9`；carousel 声明 `84c220...5fb08`、实际 `a956e8...5af65d`。在设置 `PYTHONPATH=src` 后，直接构造 `AiV2PromptRegistry` 以 `AiV2PromptRegistryError: v2 prompt template hash mismatch` 退出；`production_gate` 返回 `{"status":"blocked","error":"v2 prompt template hash mismatch"}`。这是既有 F-006 的预期复现，不是本轮文档验证新增的缺陷。
6. 相关静态检查通过：`.venv` 下 `python -m compileall -q src chat2api`、`node --check static/app.js`、`git diff --check` 均退出 0。生产 gate/release gate 与依赖 registry 的 prompt registry 测试仍因 F-006 阻断；该结果作为限制记录，不通过改配置消除。
7. T05/T06 建立的分层语义保持完整：`docs/current-state.md` 是当前事实导航；旧代码地图、运维、部署、审批和 handoff 明确标为历史/多时点来源并回链当前索引；`AGENTS.md` 保留 fail-closed、高风险授权、公开投影、唯一 caller 和真实能力未验证约束。未发现需要在本轮猜测业务意图或删除有效限制的冲突。

## 实际修改与原因

- 新增本报告，保存路径、命令、链接、hash、加载和台账检查结果，并区分通过、已知阻断和未运行范围。
- 将 `T07-001` 在 `QUEUE.csv` 标为 `done`，下一入口改为必须在新会话执行的 `T08-001`；保留 F-006/F-007/F-009/F-010 及外部能力限制。
- 更新 `STATE.md` 的当前任务、工作树差异、完成摘要、限制和下一步；修正此前“仅有 progress/审计材料差异”的过时摘要，使其与实际 Git 状态一致。
- 更新 `docs/current-state.md` 的审计交接段落，将下一步从 T07 修正为新会话 T08，并保留 F-006 等限制。
- 在 `coverage.csv` 为受本轮检查的规则、配置、部署/审批和审计记录补上 T07 证据入口；未把外部部署或真实 AI 误记为通过。

## 采用、调整和未采用建议

| 建议 | 处置 | 理由 |
|---|---|---|
| 用当前索引、入口和历史回链做定向链接/语义验证 | 采用 | 本轮检查证明路径与分层可复核 |
| 将 registry `production` 字段或历史 gate 数字当作当前运行通过 | 不采用 | 当前 static/carousel hash mismatch，且历史记录来自其他 ref/时间 |
| 在 T07 顺手修复 registry hash、生产 gate 测试或 live 配置 | 不采用/延期 | 属于 F-006/配置或程序修复，超出当前文档验证授权 |
| 处理或重写 `progress.md` | 不采用 | 用户明确排除；保留其现状和既有读取安排 |
| 在当前会话执行 T08 | 不采用 | 任务卡和用户要求均要求由新会话独立验收 |

## 执行的检查、结果及日志位置

- 队列计数、CSV 解析和依赖检查：通过（17 行队列、14 行 coverage、10 行 findings；依赖无缺失）。
- 指南/reference hash：通过（一致）。
- 12 个目标文档本地 Markdown 链接：通过（0 条断链）。
- Prompt hash/registry 加载：按预期失败（F-006）；未修改配置。
- `python -m compileall -q src chat2api`：通过。
- `node --check static/app.js`：通过。
- `git diff --check`：通过；CRLF 转换仅为 Git 提示。
- `python -m creative_studio.ai_v2.production_gate`：失败并报告 `blocked / v2 prompt template hash mismatch`；保留为已知阻断。
- `python -m creative_studio.ai_v2.release_gate`：`v2-contract: failed`；原因仍为 registry 无法加载，未视为文档验证通过。
- `tests.test_ai_v2_prompt_registry`：6 项中 2 项通过、4 项因默认 registry hash mismatch 出错。
- `tests.test_ai_v2_production_gate`：4 项中 3 项失败、1 项出错；测试夹具在 registry 初始 hash 校验处被阻断，属于同一 F-006，不新增修复。
- 未运行全量 unittest、真实 AI/图片、浏览器、服务器、公网和部署写入检查；这些均不属于本轮 T07 文档定向验证。

## 未完成项与下一任务

- F-006：需要业务/配置责任人确认 static/carousel 正确模板快照后，另行修复 registry/hash 并回归 gate；本轮不改。
- F-007/F-009：当前索引已缓解状态歧义，但最终 Prompt 批准、批准人/时间和当前 ref 对应关系仍待独立证据。
- F-008/F-010：外部服务器、live 授权、备份/回滚和受控 smoke 未验证。
- F-003/F-004/F-005：程序接口、版本指纹和恢复 containment 修复保持 deferred。
- `T08-001`：下一新会话从当前规则入口和 `acceptance-questions.md` 独立回答问题，再回看本报告和既有证据；当前会话不执行。

## 对其他任务的影响

T08 可直接从 `AGENTS.md`、`docs/current-state.md`、`docs/repo-audit/STATE.md`、`QUEUE.csv` 和 `acceptance-questions.md` 开始；必须把 F-006 的 gate 阻断、历史快照限制和未验证外部能力作为当前限制，而不能只读取历史“通过”数字。任何修复 registry/hash 的后续任务完成后，应重新执行 T07 定向验证，再由新会话执行 T08。

## 完成条件逐项核对

- [x] 检查实际修改范围，未覆盖用户工作或引入未授权业务改动。
- [x] 检查本批规则入口、文件路径、命令、接口边界、引用和关键语义。
- [x] 对可执行检查区分通过、失败、已知阻断和未运行；未把 F-006 伪装成通过。
- [x] 更新 T07 报告、QUEUE、STATE、coverage 和当前事实索引，保留现有 findings 与限制。
- [x] 没有已知的文档断链、半迁移或语义丢失。
- [ ] 新会话独立接手和全局验收：交给 T08。

## 下一轮最小必读资料与下一动作

读取 `AGENTS.md`、`docs/current-state.md`、`docs/repo-audit/STATE.md`、`QUEUE.csv`、`acceptance-questions.md` 和本报告；在新会话执行 `repo-audit-kit/tasks/T08_fresh_acceptance.md`。不要在 T08 前修订历史 handoff 或处理 `progress.md`。
