# T08-001 新会话独立验收

## 任务 ID、目标与范围

本轮执行 `repo-audit-kit/tasks/T08_fresh_acceptance.md`。这是在 T07 之后新会话进行的独立接手检查，范围包括：从当前规则和正式文档回答验收问题、核对规则加载可观察性、程序化检查全局台账，以及确认 T05/T06 的文档迁移没有遗失有效约束或未完成事项。本轮不修改业务代码、Prompt、registry、部署配置或 `progress.md`。

## 开始和结束基线 / 差异

- 当前仓库：`D:/code/ai_creative_studio`，分支 `master`，HEAD `be89f8a5ec7d8103c83ad86bf49dbb95971ff70a`。
- 工作树已有 T05/T06 文档差异、用户修改的 `progress.md`、未跟踪的 `docs/repo-audit/`、`repo-audit-kit/` 及 zip；本轮未覆盖、清理或重置这些差异。
- 附件 `C:/Users/admin/Downloads/AI仓库盘点与文档规则整理执行指南.md` 与仓库 `repo-audit-kit/reference/AI仓库盘点与文档规则整理执行指南.md` 的 SHA-256 均为 `E34CF9F11D4D921F66A329BE283877AEF51E876187452F78F02BAC32B8B780D1`。

## 独立验收答案（先于读取既有任务结论）

### 1. 正式网页与 AI 网关入口

- 正式网页的 Python 入口是 `src/creative_studio/app.py` 的 `create_application()` 及其模块启动段；默认监听 `127.0.0.1:8775`（同文件的 `run()` 默认端口和 `README.md`“使用”段）。
- AI 网关入口是 `chat2api/`，由 `launcher.py` 的子进程环境启动；启动器常量 `WEB_URL=http://127.0.0.1:8775/`、`GATEWAY_URL=http://127.0.0.1:8780`，并在 `_environment()` 注入 `PORT=8780`。运维文档“当前边界/启动”段与源码一致。
- 当前只允许本机访问；不能把监听地址改为局域网或公网，除非另开高风险变更卡并完成认证、HTTPS、限流、防火墙和回滚评审。

### 2. 修改一个 AI v2 生成用例时的真实入口

先定位对应的正式 caller 和用例模块：`src/creative_studio/ai_v2/application.py` 组合 `narrative.py`、`static_visual.py`、`carousel_visual.py`；再核对 `config/ai_v2/prompts/registry.json` 的 `path`、`template_sha256`、`input_schema`、`output_schema`、`lifecycle`、`caller` 和预算字段。随后读取 `src/creative_studio/ai_v2/schema.py`、对应 validator/mapper/store、公开 DTO/API（`src/creative_studio/ai_v2/api.py` 及 application facade）和相应用例测试（`tests/test_ai_v2_*.py`）。生产 caller 必须是 registry 声明且由 `production_gate.py` 检查可达的 canonical caller；`application.py` 中的 deterministic fake 只作为 contract seam，不是生产 caller。

设计参考应与正式入口分开：`docs/ai-rebuild-master-plan.md`、`docs/adr/`、`docs/superpowers/specs/` 和历史 handoff 解释目标、决策或过去快照，不能替代当前源码、registry、schema、测试结果和审批证据。

### 3. 未完成范围和阻塞

- 全局任务队列中 T01、T02（含 T02-007 的 `not_applicable`）、T03、T04、T05-001、T06-001、T07-001 已完成；当前 T08-001 是本轮验收任务。
- F-006 仍是明确阻塞：registry 声明的 static/carousel 模板 hash 与当前文件实际 hash 不一致，`AiV2PromptRegistry` 初始化失败，production/release gate 不能通过。该问题属于配置/程序修复，未在文档整理范围内修改。
- F-007/F-008/F-009/F-010 仍需分别保留当前索引与历史分层、部署 ref 对应关系、最终 Prompt 批准证据、live 授权和受控验证；当前索引缓解了歧义，但没有替业务责任人作批准判断。
- F-001/F-003/F-004/F-005/F-006 为 deferred 或受限后续项；真实 AI/图片、浏览器移动视口、外部服务器、公网和部署写入仍未验证。`progress.md` 是历史时间线，本轮按用户要求不处理。

### 4. 下一步和本轮禁止顺手做的动作

本轮按队列执行 `T08-001`；验收完成后没有新的审计任务，后续应在 Prompt/hash、审批、部署或程序缺口实际变化时建立有边界的增量任务。根据 `RUN_ONE_TASK.md`、`AGENTS.md` 和用户授权，本轮不能顺手修复 registry/hash、修改 launcher 或业务接口、升级依赖、部署/开放公网、发送真实平台写请求、处理 `progress.md`，也不能把历史 handoff、deterministic gate 或 GUI 启动表述为生产批准。

### 5. 最小验证的环境前提与未验证范围

最小静态/契约检查需要在仓库根目录运行，使用项目 `.venv`，并设置 `$env:PYTHONPATH = "D:\\code\\ai_creative_studio\\src"`；Node.js 用于 `node --check`。`chat2api/.env`、有效令牌、外部网络、真实模型和服务器不是静态检查的前提，但 live/部署验证另有授权与备份要求。

已执行的检查：

- `.venv` 下 `python -m compileall -q src chat2api`：退出 0。
- `node --check static/app.js`、`node --check static/ai-v2/app.js`：均退出 0。
- `git diff --check`：退出 0；仅有 Git 的 CRLF 转换提示。
- `python -m creative_studio.ai_v2.production_gate`：退出 1，输出 `{"status": "blocked", "error": "v2 prompt template hash mismatch"}`。
- `python -m creative_studio.ai_v2.release_gate`：退出 1，输出 `v2-contract: failed`、`boundary: ok`、`real_model_quality: not-run`；同时出现测试临时 SQLite 文件清理的 Windows `PermissionError`。
- `python -m unittest discover -s tests -q`：退出 1；输出包含同一 registry hash mismatch 和测试临时 SQLite 文件清理错误，不能记为全量通过。

未执行且不能由上述命令证明的内容：真实 AI/图片请求、浏览器交互和移动视口、外部网关出口、服务器文件/进程切换、公网访问、备份恢复演练及最终业务审批。

## 台账和引用核对

- `QUEUE.csv` 解析得到 17 行任务，所有 `depends_on` 均能解析到现有任务；分组状态为 T01 done=1、T02 done=6/not_applicable=1、T03 done=3、T04 done=2、T05 done=1、T06 done=1、T07 done=1、T08 done=1（本报告保存后）。
- `coverage.csv` 有 14 条记录，覆盖 T05/T06/T07 涉及的规则、当前索引、部署/审批文档和审计材料；`findings.csv` 有 10 条记录，open/deferred 项仍有 owner_task 和验证入口。
- 目标正式文档的本地 Markdown 链接检查：0 个缺失文件、0 条断链。
- registry hash 独立复核：narrative 声明与实际均为 `70b3aa2228b0e6509badc656e9eec96f5e27d77b35aba43e41703729f0cf0e4e`；static 声明 `c820c9eb6dcf95a444c9ecd490e03b09708e3280341a9f8552ca09a7dc51139b`、实际 `fb2b098954d5e7db2b6a0eba4111bcc11885e17217508fc947f8b2eed64b6fe9`；carousel 声明 `84c220d8dea03e5f036bd92c0536c706ffb88c088dd42acb1c98c63fec15fb08`、实际 `a956e80fb22b686f5f246cac070bfb271c6e43316b6839ee4c934883bd5af65d`。

## 与既有 T05/T06/T07 证据的比较

独立答案与既有报告的入口、registry 阻塞、live 边界、历史分层和未验证范围一致；没有发现需要新增发现或回滚迁移的遗漏。T05 保留的 fail-closed、唯一 caller、高风险授权和 deterministic fake 约束仍可从 `AGENTS.md` 及源码复核；T06 建立的 `docs/current-state.md` 仍是当前事实唯一导航，旧 operations/代码地图/deployment/handoff 只作为分层历史来源。T07 的链接、命令和台账结论在本轮重新得到支持。

## 采用、调整和未采用建议

| 建议 | 处置 | 理由 |
|---|---|---|
| 用短当前索引连接规则、实现、任务状态和历史来源 | 采用并复核 | 新会话可从 `AGENTS.md`、`docs/current-state.md`、STATE、QUEUE 定位入口 |
| 将指南示例或外部工具机制直接升级为项目强制规则 | 不采用 | 附件与仓库指南只是方法依据；项目事实必须回到当前源码、配置和实际检查 |
| 把 registry `production` 字段或历史 gate 数字当作运行就绪 | 不采用 | static/carousel hash mismatch，且真实质量/审批未验证 |
| 在 T08 顺手修复 hash、程序缺陷、部署或 `progress.md` | 不采用/延期 | 超出本轮文档验收范围和用户明确限制 |

## 完成条件逐项核对

- [x] 新会话从当前规则、状态和正式文档独立回答五项验收问题，并附源码/文档证据。
- [x] 核对实际可观察的规则入口；未把“AI 自述读到规则”当作加载证明。
- [x] 程序化检查队列依赖、覆盖/发现记录、目标文件和本地链接。
- [x] 复核 T05/T06/T07 的迁移语义、有效约束、例外和未完成事项去向；未发现半迁移或遗失。
- [x] 如实记录 compile/node/diff 通过、gate/全量测试失败和真实能力未运行。
- [x] 保留 F-006 等限制，未修改业务代码、Prompt、registry、部署配置或 `progress.md`。
- [x] 更新 T08 报告、QUEUE、STATE 和当前事实索引；状态为 `complete_with_limits`，不是无条件 complete。

## 下一步

本次审计约定范围的文档整理与独立接手验收已结束，但保留 F-006、审批/部署/live 证据和程序 deferred 项。维护触发条件仍以 `docs/current-state.md` 为准；当 registry、caller、schema、端口、部署 ref、审批或验证结果变化时，建立对应增量复核，并在必要时重新执行 T07 后再进行新的独立验收。
