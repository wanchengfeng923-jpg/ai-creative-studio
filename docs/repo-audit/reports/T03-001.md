# T03-001 根规则、状态入口、运维与 AI 文档核对

## 任务 ID、目标与范围

独立核对 `AGENTS.md`、`docs/project-rules/CODE_STYLE.md`、`README.md`、`docs/project-rules/CONTEXT.md`、`docs/project-rules/项目代码地图.md`、`CHANGELOG.md`、`docs/project-rules/operations.md`、`docs/ai-rebuild-master-plan.md` 的路径、命令、职责、安全约束、当前状态和实现证据；排除 `progress.md`，不修正文档。

## 基线与实际读取

基线 `HEAD be89f8a`。回查 `src/creative_studio/app.py`、`backup.py`、`ai_v2/prompt_registry.py`、`application.py`、`config/ai_v2/prompts/registry.json`、模板 SHA-256、T02 报告与 199 项 unittest 结果。

## 已证实结论

1. 路径与边界大体一致：网页 `127.0.0.1:8775`、网关 `127.0.0.1:8780`，正式前端为 `static/index.html/app.js/styles.css`，AI v2 位于 `src/creative_studio/ai_v2/`；`app.py` 与 launcher 的默认回环绑定和 v2 路由证据支持这些声明。
2. `README.md`、`AGENTS.md`、`docs/project-rules/CODE_STYLE.md`、`docs/project-rules/operations.md` 都正确要求 `PYTHONPATH=src`、unittest/compileall/node 检查、临时测试数据和真实 AI/图片未验证的诚实标注。运行命令实际受 F-006 registry 哈希错误及缺少 `curl_cffi` 影响，不能写成全套通过。
3. 备份文档描述了 dry-run、manifest 校验、临时恢复和不自动调度；代码/测试支持 manifest 路径逃逸和篡改拒绝，但 F-005 已证明 `restore_backup` 的目标目录 containment 方向仍有程序缺陷。文档可作为意图和操作入口，不能证明该缺陷已修复。
4. 文档组存在实质状态冲突（新增 F-007）：`docs/project-rules/项目代码地图.md:150-152` 与 `docs/project-rules/operations.md` 将三份 v2 prompt 描述为 candidate/caller=null；当前 `registry.json` 三项却标记 `production` 且声明 caller。另一方面，当前 registry static/carousel hash 不匹配模板（F-006），因此“production”本身也无法正常加载。
5. `docs/ai-rebuild-master-plan.md` 的 Phase 5+ 当前状态声称 registry governance、254 项 deterministic unittest 和 gate 已通过；当前可复现实测为 199 项，3 failures/44 errors，且 registry 初始化失败。该状态声明应视为历史/待重新核对，不能作为当前通过证据。
6. AI 总纲的治理约束与实现边界多数兼容：registry fail-closed、caller/版本/hash、公开 DTO 隐私、deterministic fake 与真实供应商分离、旧路径不恢复；但总纲还保留若干历史目标/旧生产 caller（如 Phase 2 `creative.narrative.generate@v6`）而当前 v2 caller 是 `creative_studio.ai_v2.*.generate`，需在整理阶段标注来源和适用版本。

## 推断与待确认意图

- `docs/project-rules/项目代码地图.md`/`docs/project-rules/operations.md` 可能未随 registry 从 candidate 切换到 production 同步更新，或 registry 文件来自另一快照；需 T04 复核 composition root、caller 与评测报告的实际关系。
- F-005 的修复是否进入正式文档承诺需要用户/后续专项决定；本轮不改文档或程序。

## 实际修改与检查

仅新增本报告，更新 `coverage.csv`、`findings.csv`、`QUEUE.csv`、`STATE.md`；未修改正式规则、文档或 `progress.md`。执行了文档关键声明检索、源码/配置回查和 T02 已保存测试证据复核。

## 未完成项与下一动作

- `T03-002`：核对部署与发布文档组。
- `T03-003`：核对 ADR、research 与受限 superpowers 当前入口。
- F-005/F-006 deferred；F-007 由 T04-001/T04-002 继续交叉复核。

## 完成条件逐项核对

- 范围内路径、命令、安全/职责声明：已核对或标明限制。
- 过期/冲突/缺少证据：F-005、F-006、F-007 已保留来源与影响。
- 未修改正式文档，未处理 `progress.md`。

## 下一轮最小必读资料与下一动作

读取本报告、T02-003/T02-006、队列与 T03-002 任务卡，执行 `T03-002`。
