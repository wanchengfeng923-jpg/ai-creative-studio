# T02-005 测试分组与夹具边界盘点

## 任务 ID、目标与范围

盘点 `tests/**` 的 unittest 入口、测试分组、夹具/临时数据隔离和当前运行边界；不修复程序、依赖或测试，不处理 `progress.md`。

## 开始和结束基线 / 差异

- 仓库基线：`HEAD be89f8a`；本轮未修改产品源码或测试。
- 工作区原有 `M progress.md` 与审计材料未跟踪状态保留。

## 实际读取与核对的关键资料

- `tests/conftest.py`：把 `src` 插入 `sys.path`，没有全局 pytest fixture。
- `tests/test_ai_v2_*.py`：v2 schema/use case/store/queue/image/session/API/前端/生产门禁分组。
- `tests/test_app_api.py`、`test_auth*.py`、`test_backup.py`、`test_repository.py`、`test_project_projection.py`：核心应用、认证、备份、仓储和投影分组。
- `tests/test_chat2api_web_client.py`、`test_launcher_proxy.py`、`test_deployment_paths.py`、`test_retired_ai_cleanup.py`、`test_tag_options.py`：网关客户端、启动/部署路径、退役清理和配置标签分组。

## 已证实结论及证据

1. 入口是 `python -m unittest discover -s tests -v`；无额外测试配置文件，`conftest.py` 仅承担源码导入路径准备。
2. 绝大多数需要数据库的测试使用 `tempfile.TemporaryDirectory()` 创建临时 SQLite；`setUp/tearDown` 或 `with` 负责生命周期。集成测试也以临时目录注入数据库/图片目录，未见将测试写入仓库 `data/` 的直接证据。
3. 运行命令（`PYTHONPATH=src`）发现 199 个测试：`FAILED (failures=3, errors=44)`。44 个错误中大部分由 `AiV2PromptRegistryError: v2 prompt template hash mismatch` 触发，和既有 F-006 一致；`test_chat2api_web_client` 因环境缺少 `curl_cffi` 导入失败。3 个生产门禁断言失败也先在 registry 初始化错误处暴露。
4. 运行结果不代表真实 AI、图片网关、浏览器或部署验证；本任务仅记录测试边界和可复现命令。

## 推断与待确认意图

- 现有测试结构已覆盖从纯函数到临时 SQLite 的多层 seam，但运行前提（源码路径、可选网关依赖、registry 模板哈希）没有由单一测试入口自动检查。
- 是否将 `curl_cffi` 纳入默认开发/CI 安装集、以及 F-006 的 registry 来源/更新策略，留给 T03/T04 文档核对和后续修复授权。

## 实际修改与原因

仅新增本审计报告，并更新 `coverage.csv`、`QUEUE.csv`、`STATE.md`；未修改正式规则、文档、程序、测试或 `progress.md`。

## 执行的检查、结果及日志位置

- `PYTHONPATH=D:\\code\\ai_creative_studio\\src python -m unittest discover -s tests -v`
- 完整输出保存于临时日志：`C:\\Users\\admin\\AppData\\Local\\Temp\\repo-audit-t02-005-unittest.log`。
- 结果：199 tests，3 failures，44 errors；未宣称通过。

## 未完成项与拆分后的任务 ID

- `T02-006`：config 与 Prompt/Schema/Eval registry 配置盘点。
- F-006 继续由 `T02-006` 核对，程序修复保持 deferred。
- `curl_cffi` 缺失属于环境/依赖核对项，暂不新增修复任务。

## 对其他任务的影响

为 T03/T04 提供测试入口、隔离边界和失败证据；T03/T04 必须区分 deterministic/临时夹具通过与真实服务未验证。

## 完成条件逐项核对

- 测试分组、入口和夹具边界：已记录。
- 运行验证与失败原因：已记录并保留日志入口。
- 未覆盖范围：真实 AI/图片/浏览器/部署明确列为未验证。

## 下一轮最小必读资料与下一动作

继续使用 `RUN_ONE_TASK.md`，最小读取本报告、`QUEUE.csv`、`coverage.csv`、`findings.csv`、`AGENTS.md`，执行 `T02-006`；不重跑已完成的 T02-001 至 T02-005。
