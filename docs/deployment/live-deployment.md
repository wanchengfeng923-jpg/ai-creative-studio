# AI 创意工作台实时部署记录

状态：步骤 0 完成；步骤 1 盘点进行中
建立日期：2026-09-06
仓库：`D:\code\ai_creative_studio`
分支：`codex/tag-accordion-prototype`

## 使用规则

本文档是本次部署的唯一实时记录。每完成一步，补充执行时间、操作者、证据、未通过项和回滚点，再进入下一步。没有完成验证，不标记为完成。

已获用户确认：以当前本机项目和 `data/` 为权威源，迁移程序、数据库、图片和上传文件到指定服务器，允许覆盖目标同名旧数据，并采用下述 E 盘目录。实际切换仍须完成盘点、备份、恢复验证和目标核对；本机源数据保持可用。

当前处于只读盘点阶段，尚未执行上传、覆盖或服务配置。敏感配置处理、对外入口和自动启动方案在对应步骤落实；不提交当前工作树。

## 目标

将 AI 创意工作台及本机业务数据迁移到公网 Windows 单服务器 `42.194.220.18`，继续使用服务器本地 SQLite，并在每一步保留可验证的回滚路径。

## 当前事实

| 项目 | 当前状态 | 证据/位置 |
|---|---|---|
| 网页服务 | 本机监听 `127.0.0.1:8775`；截图提供公网候选 `42.194.220.18:8775` | 本机进程；用户提供的聊天截图 |
| AI 网关 | 当前监听 `127.0.0.1:8780` | `chat2api/main.py` 进程 |
| AI live 环境变量 | 当前 shell 未设置 | 本次检查 |
| 生产配置文件 | 存在，不打印内容 | `chat2api/.env` |
| 业务数据库 | 本机 SQLite | `data/creative_studio.db` |
| 图片数据 | 本地文件目录 | `data/images/` |
| 上传数据 | 本地文件目录 | `data/uploads/` |
| 备份工具 | 已有，尚未配置自动调度 | `creative_studio.backup` |
| 外部访问 | 仓库默认未开放；截图提到公网端口 `80` 和候选 `8775`，尚未从服务器侧验证 | 用户提供的聊天截图；待网络门禁验证 |
| 公网 TCP 探测 | `3389`、`5985` 可达；`80`、`8775`、`5986` 不可达 | 2026-09-06 本机只读 `Test-NetConnection`；仅证明端口连通 |
| 目标服务器身份 | 主机名 `10_10_2_15`；已通过 Windows RDP 登录 | 用户在服务器 PowerShell 提供的只读输出 |
| 目标服务器磁盘 | C 盘可用约 33.14 GB；E 盘可用约 99.89 GB；D 盘未显示容量 | 用户在服务器 PowerShell 提供的只读输出 |
| 目标服务器监听 | `3389`/`5985` 监听；`80`/`8775`/`8780` 未监听 | 用户在服务器 PowerShell 提供的只读输出 |

## 部署边界

- 软件本体：仓库代码、虚拟环境、静态资源；可独立替换。
- 用户运行数据：登录会话、缓存、日志和任务状态；不得随软件包覆盖。
- 业务数据：SQLite 数据库；不得通过共享目录供多台服务器直接并发打开。
- 文件数据：图片和上传文件；迁移前必须按清单和哈希校验。
- 敏感配置：`chat2api/.env`；不进入 Git、日志、备份包展示或聊天内容。

## 分步计划

| 步骤 | 内容 | 状态 | 进入条件 |
|---|---|---|---|
| 0 | 锁定目标环境、权威数据源和部署范围 | 完成 | 用户已确认本机权威源、覆盖范围和 E 盘目录 |
| 1 | 盘点服务器、目录、端口、计划任务和数据库读写者 | 进行中 | 已收到身份、磁盘和部分监听端口输出；目录、任务和读写者待盘点 |
| 2 | 生成并验证本次部署前备份 | 未开始 | 步骤 1 完成 |
| 3 | 确定拓扑：单机隔离或应用/数据库分机 | 未开始 | 资源和故障边界明确 |
| 4 | 制作独立版本包并完成离线验收 | 未开始 | 版本和回滚点明确 |
| 5 | 受控部署与服务启动 | 未开始 | 备份可恢复、停止写入者 |
| 6 | 业务验收：登录、核心流程、文件和 AI 链路 | 未开始 | 新服务已启动 |
| 7 | 观察期、回滚演练和收尾 | 未开始 | 验收证据完整 |

## 步骤 0：已完成

### 确认结果

1. 目标是本机正式运行、内网服务器，还是公网服务器？【已确认：公网服务器】
2. 目标服务器：Windows，公网 IP `42.194.220.18`，主机名 `10_10_2_15`；用户已通过 RDP 登录。WinRM 只确认 TCP 可达和监听，未验证认证命令往返。
3. 本次是只部署程序，还是包含数据库、图片和上传文件迁移？【已确认：全部迁移】
4. 权威源：当前本机项目和 `data/`；用户已确认允许覆盖目标同名旧数据，覆盖前先核验并保留回退点。
5. 是否继续使用 SQLite，还是准备迁移到 PostgreSQL/MySQL/SQL Server？【已确认：继续使用 SQLite；单服务器】
6. 不需要正式域名；用户询问了反向代理含义，尚未决定是否采用。HTTPS/传输保护、反向代理和开机自动启动留待部署方案步骤确定。

### 步骤 0 验收

- [x] 目标环境已明确
- [x] 部署范围已明确
- [x] 权威数据源和目标同名旧数据覆盖授权已明确
- [x] E 盘目录规划已确认
- [x] 当前仅盘点和更新文档，未执行远程写入

回退副本实际位置、保留期限和恢复验证结果在备份步骤补全，未完成前不得切换。

### 已确认的步骤 0 结论

- 目标：公网 Windows 服务器。
- 范围：程序、SQLite 数据库、图片和上传文件全部迁移。
- 数据库：继续使用 SQLite；服务器上由唯一服务进程打开，不能通过共享目录并发访问。
- 入口：不配置正式域名；反向代理方案待定。公网入口仍需完成防火墙、认证、HTTPS/传输保护和回滚验证。
- 截图中的 `42.194.220.18:8775` 只作为待验证的网页入口候选；同事提到的 `80` 只作为待确认的 HTTP 端口，不能当作 RDP 管理端口。
- 用户已确认通过 Windows 自带远程桌面（RDP）登录服务器；公网 `3389` 与此前 TCP 探测结果一致。
- 用户已同意目录：程序 `E:\AI-Creative-Studio\app\`、数据 `E:\AI-Creative-Studio\data\`、暂存 `E:\AI-Creative-Studio\staging\`。目录尚未创建或核验；配置、日志和备份目录在步骤 1 补充。独立数据路径是否已被应用支持仍需从代码核实。
- 文件上传优先采用 RDP 本地磁盘映射；大文件先打包并生成 SHA-256，上传到服务器暂存目录，校验通过后再解压和切换。

## 变更记录

| 时间 | 步骤 | 操作 | 结果 | 证据 | 回滚点 |
|---|---|---|---|---|---|
| 2026-09-06 | 初始化 | 创建实时部署文档并记录本机状态 | 完成 | 本文件；测试与 gate 已通过 | 未发生变更 |
| 2026-09-06 | 步骤 0 | 确认本机权威源、目标覆盖范围和 E 盘目录 | 完成，进入步骤 1 | 用户回复“1.确认2.同意” | 未执行迁移；本机源保持可用 |
| 2026-09-06 | 步骤 1 | 核验服务器目标目录存在性 | `E:\AI-Creative-Studio` 尚不存在；E 盘根目录清单尚未收到 | 用户提供服务器 `Test-Path` 输出 | 只读检查，未创建目录 |
| 2026-09-06 | 步骤 1 | 用户确认服务器为全新实例，要求跳过旧环境盘点 | 按全新目标处理，不再要求旧应用、旧任务和旧数据清单 | 用户明确说明“新开的服务器，不用确认了” | 无旧部署回滚；切换前保持本机源可用 |
| 2026-09-06 | 步骤 1 | 接入可配置数据根目录 | 定向路径测试通过；默认路径保持不变；启动器传递数据和网关目录变量 | `tests/test_deployment_paths.py`；compileall；diff-check | 尚未上传或切换服务器 |
| 2026-09-06 | 步骤 2 | 生成部署前备份并恢复验证 | 备份完成；恢复 smoke `verified=true`、`references_verified=true`；恢复库含 3 用户（2 admin、1 user） | `.scratch/deployment-backup-20260906`、`.scratch/deployment-restore-smoke-20260906` | 本机原始数据未修改 |
| 2026-09-06 | 步骤 2 | 生成不含敏感配置的程序包 | 完成；6432 个条目；未发现 `.env`、数据库、图片、上传文件、虚拟环境或 scratch 条目 | `.scratch/ai-creative-studio-deployment-20260906-v2.zip`；SHA-256 `6515DA374282A906BA4BF4CC6CEC69456EFA9479DEDC6919497BFD2034291178` | 服务器尚未接收或切换 |

## 当前步骤：步骤 1

- [x] 收到服务器身份、磁盘可用容量和指定端口监听输出
- [x] 核验目标目录是否存在：`E:\AI-Creative-Studio` 尚不存在
- [x] 按用户确认的全新服务器处理，跳过 E 盘旧目录盘点；不是工具扫描结论
- [x] 按用户要求跳过旧应用、非系统任务和旧数据库读写者盘点
- [ ] 核实 Windows 版本、CPU、内存和所需运行时
- [ ] 盘点本机待迁移数据量、路径配置和网关持久状态
- [ ] 确定传输通道；RDP 登录可用不代表已启用磁盘映射，WinRM 尚无认证往返证据
- [ ] 形成源到目标清单和峰值空间估算后进入备份步骤

### 步骤 1 实现准备结果

- `CREATIVE_STUDIO_DATA_DIR` 可指定工作台数据库、图片和上传数据根目录；未设置时仍使用仓库 `data/`。
- 启动器为服务器部署预留 `CHATGPT_IMAGES_DIR` 和 `CHATGPT_IMAGE_JOB_DIR`，实际服务器值将在部署配置步骤注入。
- 定向路径测试 1 项通过；compileall 和 `git diff --check` 通过。
- 全量测试本次重跑出现既有 Prompt deterministic fixture 与当前 carousel contract 不一致（`carousel-05`）以及既有 Prompt 文本断言失败；未将其归因于路径切片，发布包前必须重新解决或明确排除该工作树状态。
- 当前不生成发布包：工作树中的 Prompt registry/Prompt 正文已经是 production 变更，而部分测试仍按 candidate/旧文案断言；同时 `carousel-05` 评测期望与 deterministic fake 输出不一致。该基线冲突需单独确认后处理。
- 已按当前 production Prompt 更新评测基线：`carousel-05`/`carousel-06` 接受当前轮播用例的帧数量归一化行为，Prompt 文案断言改为当前正文，release gate 统计同步更新。全量 unittest 200 项、release gate 124 项、compileall 和 diff-check 通过。

### 本机初步盘点

以下为运行中文件的体积统计，不是一致性快照，也未生成迁移哈希。

| 位置 | 文件数 | 字节数 | 处理要求 |
|---|---|---|---|
| `data/` | 99 | 279055446 | 业务数据库、图片、上传文件，使用一致性备份 |
| `chat2api/images/` | 115 | 292028803 | 核实网关图片引用后纳入迁移清单 |
| `chat2api/image_job_state/` | 151 | 49341 | 私有网关任务状态，停写后按引用核验迁移，不展示内容 |

三处合计 571133590 字节，约 545 MiB；该数据规模无需分卷。程序、依赖和备份空间尚未计入，最终传输包大小另行核算。

`src/creative_studio/app.py` 当前使用 `ROOT_DIR / "data"`，独立 `E:\AI-Creative-Studio\data\` 尚未接入默认运行入口。打包前应完成路径适配和隔离验证，不能直接搬移后宣称可运行。网关任务目录已有 `CHATGPT_IMAGE_JOB_DIR` 配置入口，其他网关文件路径仍需核实。

### 步骤 2 结果

- 备份目录：`.scratch/deployment-backup-20260906`。
- 恢复验证目录：`.scratch/deployment-restore-smoke-20260906`。
- 私有配置不进入程序包；`.env` 通过 RDP 单独复制到服务器受保护目录，数据库迁移会保留用户和管理员账号。
- 步骤 2 已完成，下一步是通过 RDP 上传程序包和备份包到服务器暂存目录；上传前不创建正式应用目录。

### 服务器上传状态

- 用户已将程序包解压到 `E:\AI-Creative-Studio`。
- 服务器网关首次手动启动因未设置 `PORT`，尝试旧端口 `8700` 并退出；补充 `PORT=8780`、`HOST=127.0.0.1` 后启动成功。
- 服务器 `http://127.0.0.1:8780/health` 返回 `200`，`has_token=true`、`auto_refresh=true`；未记录 Token 内容。
- 用户确认开放公网网页端口 `8775`；不使用 `80`、域名或反向代理。AI 网关 `8780` 继续只监听本机。该变更只涉及监听地址和 Windows 防火墙，不改变工作台页面、数据库或 Prompt。
- 服务器只读核验：程序文件和目录结构已在 `E:\AI-Creative-Studio` 根层级展开；根目录暂无 `database`/`images` 数据目录，业务备份位置尚未确认。
- 尚未启动服务、创建数据目录、覆盖数据库或注入 `.env`。
- 下一步先只读核验目录结构和关键文件，再处理独立数据/配置。

## 验证记录

以下测试结果是此前检查时的证据，不代表后续工作树改动已验证。制作发布包前须冻结版本并对实际待发布代码重新验收。

- 2026-09-06：全量 unittest 185 项通过。
- 2026-09-06：AI v2 unittest 111 项通过。
- 2026-09-06：release gate、boundary、Node 语法、compileall、`git diff --check` 通过。
- 2026-09-06：未进行真实 AI 请求、服务器部署、数据库迁移或公网验收。

## 回滚原则

任何切换前保留旧程序、可读取的数据库备份、图片/上传文件清单和配置回退点。切换失败时先停止新写入，再恢复旧程序和旧配置，最后验证登录、核心查询和文件读取。不得只回滚程序而留下不兼容的数据结构。

## 相关文档

- `AGENTS.md`
- `docs/operations.md`
- `docs/deployment/code-baseline-20260908.md`
- `docs/ai-rebuild-master-plan.md`
- `docs/superpowers/handoffs/2026-09-06-ai-v2-current-state-handoff.md`
- `docs/superpowers/handoffs/2026-09-06-ai-v2-next-session-handoff.md`

## 2026-09-08 代码基准盘点

- 已确定开发机 Git 仓库 `D:\code\ai_creative_studio` 为唯一代码权威源；服务器目录只作为已发布运行副本。
- 已生成开发机当前工作树和 2026-09-06 部署包的代码清单；二者均为 112 个纳入文件，逐文件仅 `launcher.py` 不同。
- 开发机聚合 SHA-256：`7f29098da58e5ae8a52acd1329ab941976ee8e1a0b07fbe438fdf8e6ba114d9f`。
- 部署包聚合 SHA-256：`e0261357199ce918f5429a24e9afe2304ecca924554e2c1d8ce5b13ec33cea88`。
- 当前 Git `HEAD` 为 `1bb6dd1c7a523f2f560af5c74d5d62ec0e7fa49d`，但工作树包含大量未提交开发成果，因此该提交不能作为当前完整代码基准。
- 服务器现场指纹和 `launcher.py` 配置路径决策完成后，才创建正式基准提交/tag并进入标准发布流程。
- 服务器清单内 111 个文件与部署包逐个一致；PowerShell 5.1 因脚本编码漏扫 739 字节的中文名启动批处理。开发机启动器已按仓库规范和服务器现实收口为读取 `chat2api\.env`，未修改配置内容。
- 差异收口后开发机候选与部署包均为 112 个文件、1,141,155 字节、聚合 SHA-256 `e0261357199ce918f5429a24e9afe2304ecca924554e2c1d8ce5b13ec33cea88`；新鲜全量 unittest 200 项及静态检查通过。
- 已直接核验服务器 `启动AI创意工作台.bat`：739 字节，SHA-256 `6390ffc13978d6049fcaffaab6c47c25a81bfb1d0dad0f4d03a2a15e25d60991`。合入首次清单后，服务器完整代码指纹同为 `e0261357199ce918f5429a24e9afe2304ecca924554e2c1d8ce5b13ec33cea88`。
- 代码盘点正式完成：开发机仓库是唯一代码权威源，服务器是已发布运行副本；生产数据和生产 `.env` 继续以服务器为权威源。
- Git 基准以 annotated tag `production-baseline-20260908` 固定；仓库当前没有远端，因此该 tag 和提交暂时只保存在开发机。
- 为消除 Windows `core.autocrlf` 对代码身份判断的影响，代码盘点脚本升级为 v2：文本按 LF 规范化后计算内容指纹，`.bat` 保留原始字节。开发机与部署包的规范化结果均为 112 个文件、1,135,704 字节、SHA-256 `b929837a172cb8dfbd7de3eebd62c8c3dab6ff82f933b1bbdbe2918914156e30`。
