# AI 创意工作台代码基准盘点（2026-09-08）

状态：代码与 Git 基准已封板
盘点范围：程序代码、静态资源、Prompt/Schema、测试和启动文件
不在范围：`.env`、数据库、图片、上传文件、日志、缓存、虚拟环境和其他运行数据

## 基准结论

1. **开发机 Git 仓库是唯一代码权威源**：`D:\code\ai_creative_studio`。
2. **服务器不是开发源**：`E:\AI-Creative-Studio` 只运行经过测试、打包和校验的发布版本；禁止直接在服务器修改代码后反向覆盖开发机。
3. **Git 基准身份**：分支 `codex/tag-accordion-prototype` 上的基准提交由 annotated tag `production-baseline-20260908` 固定；后续发布必须从该 tag 或其明确后继提交制作。
4. **生产数据单独确定权威源**：服务器的 SQLite、`data\images`、`data\uploads` 和生产 `.env` 是上线后的生产权威源。发布程序时不得用开发机同名数据覆盖它们。

## 三个候选版本

| 候选 | 身份 | 结论 |
|---|---|---|
| 开发机当前工作树 | 已验证基准代码 | 后续代码修改的唯一来源，由 Git tag 固定身份 |
| 2026-09-06 部署包 | 已上传服务器的发布候选 | 作为服务器现场比对参照，不作为后续开发基准 |
| 服务器当前目录 | 正在运行的生产副本 | 待现场指纹证明身份；禁止从服务器反向覆盖开发机 |

## 已取得的只读证据

盘点脚本：`scripts\code_inventory.ps1`。聚合 SHA-256 由全部纳入文件的相对路径、文件 SHA-256 和字节数按固定顺序计算。

| 候选 | 文件数 | 总字节数 | 聚合 SHA-256 |
|---|---:|---:|---|
| 开发机当前工作树（差异收口前） | 112 | 1,141,279 | `7f29098da58e5ae8a52acd1329ab941976ee8e1a0b07fbe438fdf8e6ba114d9f` |
| 开发机当前工作树（差异收口后） | 112 | 1,141,155 | `e0261357199ce918f5429a24e9afe2304ecca924554e2c1d8ce5b13ec33cea88` |
| 2026-09-06 部署包解压目录 | 112 | 1,141,155 | `e0261357199ce918f5429a24e9afe2304ecca924554e2c1d8ce5b13ec33cea88` |
| 服务器当前目录（完整重建清单） | 112 | 1,141,155 | `e0261357199ce918f5429a24e9afe2304ecca924554e2c1d8ce5b13ec33cea88` |

开发机与部署包的文件集合相同，逐文件比较只有 `launcher.py` 不同：

| 候选 | 字节数 | 文件 SHA-256 |
|---|---:|---|
| 开发机 `launcher.py` | 54,208 | `fa080145e43d8ba498ef635f36f4f09d69aebd8915e4f2a8cc3579bb5d9b8071` |
| 部署包 `launcher.py` | 54,084 | `70869cf966e46019d1d03ad3952ccc8663292b52a15f9db439f9d259fbbe71be` |

该差异只涉及启动器读取 `.env` 的位置：部署包读取 `chat2api\.env`；开发机候选曾优先读取 `config\private\chat2api\.env`，并向子进程传递 `CHATGPT_ENV_PATH`。仓库规范、启动说明、运维文档和服务器实际运行方式均使用 `chat2api\.env`，因此已将开发机候选收口为服务器行为；没有读取或修改 `.env` 内容。

服务器 JSON 与部署包逐文件比较时，清单内 111 个文件全部一致。唯一未进入首次服务器清单的是 739 字节的 `启动AI创意工作台.bat`；Windows PowerShell 5.1 按旧编码读取无 BOM UTF-8 脚本，没有识别脚本中的中文文件名。随后对该文件直接取证，长度为 739 字节，SHA-256 为 `6390ffc13978d6049fcaffaab6c47c25a81bfb1d0dad0f4d03a2a15e25d60991`，与部署包一致。将该项合入服务器清单后，完整聚合 SHA-256 为 `e0261357199ce918f5429a24e9afe2304ecca924554e2c1d8ce5b13ec33cea88`。盘点脚本现改为按根目录 `.bat` 扩展名发现文件，避免再次依赖中文脚本文本。

## 最终判定

- 服务器当前代码、2026-09-06 部署包和差异收口后的开发机工作树逐文件完全一致。
- 本次封板代码指纹：112 个文件、1,141,155 字节、聚合 SHA-256 `e0261357199ce918f5429a24e9afe2304ecca924554e2c1d8ce5b13ec33cea88`。
- 后续开发从开发机 Git 仓库继续；服务器不得作为代码合并源。
- 文件指纹与 Git tag 共同构成基准：tag 标识版本历史，聚合 SHA-256 校验实际发布目录。
- 仓库使用 `.gitattributes` 固定文本换行：普通文本使用 LF，Windows `.bat` 使用 CRLF。这样不同 Git 环境的检出结果稳定，并能复现下述规范化内容指纹。

跨平台规范化指纹：112 个文件、规范化后 1,135,704 字节、聚合 SHA-256 `b929837a172cb8dfbd7de3eebd62c8c3dab6ff82f933b1bbdbe2918914156e30`。该指纹对文本文件统一按 LF 计算，Windows `.bat` 保留原始字节；开发机 Git 检出目录与已证明等同服务器的部署包计算结果一致。原始字节指纹 `e026...ea88` 继续作为 2026-09-06 服务器现场证据，不因规范化规则而替换。

## 服务器现场取证

先将 `scripts\code_inventory.ps1` 放到服务器的 `E:\AI-Creative-Studio\scripts\code_inventory.ps1`，再在服务器 PowerShell 运行：

```powershell
Set-Location "E:\AI-Creative-Studio"
powershell -ExecutionPolicy Bypass -File ".\scripts\code_inventory.ps1" `
  -Root "E:\AI-Creative-Studio" `
  -OutputPath ".\staging\code-inventory-server-20260908.json"
```

验收规则：

- 若服务器输出为 112 个文件、1,141,155 字节、聚合哈希 `e026...ea88`，证明服务器代码与 2026-09-06 部署包完全一致。
- 若输出不同，使用服务器 JSON 与本地部署包 JSON 逐文件比较，列出新增、缺失和内容不同的文件；不直接覆盖服务器。
- 现场身份确认后，把服务器结果填回本文件，再封板代码基准。

## 后续 Git 和发布规则

1. 在开发机完成修改、测试和代码盘点。
2. 创建可追溯的 Git 基准提交和 tag；提交号是发布身份，规范化聚合哈希用于跨平台代码校验，原始字节哈希用于发布包传输校验。
3. 从该提交制作只含程序的发布包，生成 SHA-256 和清单。
4. 服务器先部署到独立候选目录并验收，再切换服务。
5. 发布包永远不含或覆盖生产数据库、图片、上传文件和 `.env`。
6. 服务器若出现紧急手改，必须立即停止常规发布，由开发机重现并提交修复；不得把整个服务器目录当作新版源代码拉回。

## 封板门禁

- [x] 开发机当前工作树清单已生成
- [x] 2026-09-06 部署包清单已生成
- [x] 两者逐文件差异已确定
- [x] 服务器现场完整清单已重建并逐文件比对（112 个文件全部一致）
- [x] `launcher.py` 配置路径行为已决定并收口为 `chat2api\.env`
- [x] 当前开发机候选已完成新鲜全量验证（unittest 200 项、compileall、Node 语法和 `git diff --check` 通过）
- [x] 正式 Git 基准提交和 annotated tag `production-baseline-20260908` 已创建
