根据提供的展示方案和已完成画面，只规划待生成的一张后续画面，并为图片模型准备一条可直接执行的生图指令。

输入上下文是 JSON，包含：方案的完整视觉 brief（核心主体、画面布局、视觉风格、创意来源、内容延展、参考来源、关键词）、完整 frame_plan、visual_continuity_rules、已完成画面、上一张画面信息、待生成序号和目标画幅。上一张实际图片不在文字模型请求中，而由服务端单独传给图片生成模型作为参考图。

上下文：{{context}}

只输出一个合法 JSON 对象，根对象只能包含 frame，不要输出方案说明、创意来源、完整路线、分析、Markdown 或其他字段。

严格使用以下结构：
{
  "frame": {
    "frame_index": 2,
    "content": "当前画面的静态内容",
    "transition_from_previous": "从上一张实际画面承接到当前画面的关系",
    "transition_to_next": "当前画面到下一张的承接信息，最后一张必须为 null",
    "ending_note": null,
    "image_generation_instruction": "直接提交给图片生成模型执行的当前画面生图指令；必须要求输出图片，不得回答解释文字"
  }
}

只返回待生成序号对应的一张画面。必须参考方案完整视觉 brief、上一张画面信息、已完成画面记录和方案级视觉连续性规则。image_generation_instruction 必须包含目标画幅、主体、布局、风格、产品证据、与上一张的连续性和必要的负面限制；它会被原样提交给图片生成模型，必须写成“生成一张图片”的执行指令，不得让图片模型返回文字、JSON、Markdown 或解释。该字段是服务端隐藏字段，不进入浏览器响应或公开历史。
