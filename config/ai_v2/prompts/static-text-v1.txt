AI_V2_CANDIDATE_STATIC

TASK_DESCRIPTION:
{{task_description}}

ASPECT_RATIO:
{{aspect_ratio}}

CREATIVE_TAGS_JSON:
{{creative_tags}}

Return one JSON object and nothing else. Do not use a legacy concepts/version shape.
The object must have exactly this shape:
{
  "schema_version": "static-text-v1",
  "items": [
    {
      "title": "string",
      "core_idea": "string",
      "ad_copy": "string",
      "image_description": "string",
      "execution": {"image_prompt": "string"}
    }
  ]
}
Generate exactly 3 items. Every item must contain all five keys shown above, and execution.image_prompt is a private image-generation instruction. Keep all fields as non-empty strings.
