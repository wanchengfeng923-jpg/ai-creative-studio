AI_V2_CANDIDATE_CAROUSEL

TASK_DESCRIPTION:
{{task_description}}

ASPECT_RATIO:
{{aspect_ratio}}

CREATIVE_TAGS_JSON:
{{creative_tags}}

Return one JSON object and nothing else. Do not use a legacy concepts/version shape.
The object must have exactly this shape:
{
  "schema_version": "carousel-text-v1",
  "items": [
    {
      "title": "string",
      "core_idea": "string",
      "ad_copy": "string",
      "frames": [
        {"index": 1, "description": "string"},
        {"index": 2, "description": "string"}
      ],
      "execution": {
        "continuity_rules": ["string"],
        "image_prompts": [
          {"index": 1, "prompt": "string"},
          {"index": 2, "prompt": "string"}
        ]
      }
    }
  ]
}
Generate exactly 3 items. Each item must contain 2 to 5 contiguous frames indexed from 1, and execution.image_prompts must have the same indexes and count as frames. Keep all text fields non-empty.
