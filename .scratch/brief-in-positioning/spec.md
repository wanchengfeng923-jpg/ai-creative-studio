# 任务描述并入创意定位

## 目标

- 删除独立的“任务说明”页面。
- 在“创意定位”流程顶部保留一个任务描述输入框。
- 将工作流步骤收敛为“创意定位 → 生成与采用”。

## 非目标

- 不修改 SQLite 表、API 字段或生成服务。
- 不改变标签选择、画幅选择、历史和采用行为。

## 验收

- 正式页面不存在任务类型输入框和独立任务说明步骤。
- `taskDescription` 位于 `tagControls` 之前，并继续自动保存为 `task_description`。
- 桌面 `1280x720`、移动 `390x844` 无横向溢出。

## 回滚

- 通过 Git 回退本次提交即可恢复原三步页面；运行数据不受影响。
