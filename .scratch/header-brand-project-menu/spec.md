# 顶部品牌与项目入口调整

## 目标

- 用不含字母的原创图形标识替换 `AI` 方块。
- 品牌名称改为“辅助创意工具”。
- 将项目列表入口放到左侧品牌旁，并重做文字层级与按钮样式，缩短打开项目抽屉的指针移动距离。

## 非目标

- 不改变项目 CRUD、自动保存、删除和项目名称编辑逻辑。
- 不修改 API、数据库、AI 网关、生成流程或运行数据。

## 验收

- 桌面端项目列表按钮位于品牌右侧，点击可打开左侧抽屉。
- 项目名称仍可直接编辑并触发原有自动保存。
- `1280x720` 与 `390x844` 无横向溢出，顶部信息不重叠。
- 页面无新增控制台错误，仓储最小代码检查通过。

## 回滚

- 以本分支提交为回滚边界；不涉及数据迁移。
