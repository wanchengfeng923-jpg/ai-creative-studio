# 创意定位工具组重排

## 目标

- 将展示类/叙事类切换和目标画幅从分散位置收拢到“02 创意定位”标题旁的紧凑工具组。
- 画幅仅保留横版/竖版按钮并紧贴标题右侧；展示类/叙事类切换固定在工具组最右端。
- 桌面端让标题、类型和画幅形成一条清晰的操作基线；移动端自动换行并保持触控尺寸。
- 保留现有模式切换、画幅选择、自动保存和展示类/叙事类字段行为。

## 非目标

- 不修改标签配置、章节选择、API、数据库、提示词或生成逻辑。
- 不改变模式选项值、画幅值、默认值和移动端底部操作栏。

## 验收

- “02 创意定位”标题右侧同时显示类型切换和目标画幅；顶部介绍区不再重复显示类型切换。
- 画幅控件不显示“目标画幅”标签，类型切换位于工具组最右侧，标题到画幅之间保持小间距。
- 高级生成设置中不再重复显示目标画幅；切换模式时画幅控件仍按展示类规则显隐。
- 1280px/2048px 桌面布局紧凑且不挤压标题；390px 下工具组可换行、无横向溢出。

## 回滚

- 恢复 `static/index.html` 和 `static/styles.css` 本次结构/样式改动及本变更卡；不触碰运行数据。
