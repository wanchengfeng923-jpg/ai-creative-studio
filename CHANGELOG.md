# Changelog

## 2026-09-02

- 修正标签报表：选中项保留在原位置并高亮显示，不再从选项表中消失。

## [Unreleased]

- 新增 PBKDF2 密码哈希、HttpOnly 会话、CSRF 防护、登录限流、审计和管理员普通账号管理；普通账号按项目所有者隔离数据。支持一次性 CLI 或启动环境变量创建首个管理员。

- 重构生成链路：新增共享生成模型和生成服务，`StudioApplication.generate` 仅保留 HTTP 入口职责；空白轮播提示词现在可通过归一化后继续生成展示类方案。
- 建立独立项目的智能体规则、代码地图、进度入口、运维手册和变更卡模板。
- 补充基于权威一手资料的 AI 软件工程流程研究笔记。
- 新增本地启动控制台，可保存网关配置并一键启动网页与 AI 网关。
- 启动控制台移除代理输入，检测连接增加等待/失败反馈，并支持仅用 Session Cookie 自动换取 Access Token。
- 仅填写 Session Cookie 时，点击“检测连接”即可自动启动网关并回填 Access Token。
- 修复旧 Token 已过期但非空时检测不触发 Cookie 换新的问题。
- 叙事类和展示类定位改为可选择的配置标签；展示类增加卖点→展示内容过滤、美术风格级联和轮播条件控件。
- 正式页面替换为炭灰×青绿的三步引导式工作流，保留真实项目、标签、历史、采用和图片任务能力。
- 项目导航改为顶部入口触发的覆盖式侧栏；移动端使用紧凑步骤条和底部操作区。
- 顶部品牌更名为“辅助创意工具”并使用新的原创图形标识；项目列表入口移至品牌旁，名称在选中项目卡内联编辑，切换项目保持左栏展开，点击栏外遮罩关闭。
- 正式第 2 步接入方案 C 的章节式标签流程：展示类按模块、叙事类按分类分章，支持主/辅助槽位、单章确认/跳过、定义提示及依赖联动清理。
- 主选/辅助标签改为顺序选取：首个标签自动作为主选并显示星标，后续标签自动作为辅助，移除主选时按顺序递补，避免同一组选项重复展示。
- 章节进入时默认全部收起；移除章节底部左侧完成按钮，空章节也可直接展开或收起。
- 精简正式标签章节文案：移除顶部定位摘要、展开区重复标题、顺序选择说明、额外计数条及必填未完成提示，让选项直接成为主要内容。
- 将展示/叙事类型切换与目标画幅控件移至“02 创意定位”标题旁的紧凑工具组；高级生成设置不再重复画幅控件。
- 进一步精简定位工具组：移除“目标画幅”标签，横/竖版按钮贴近“创意定位”标题，展示/叙事切换靠右对齐。
- 收紧标签选项网格：桌面单项宽度限制在约 190px、间距缩至 6px，移动端保留单列和触控高度，避免选项被拉伸。
- 正式页面桌面工作区改为宽屏密度布局：章节闭合时两列排列，展开章节横跨整行，标签选项按桌面宽度使用三列/四列网格；第 3 步结果区占满可用宽度，移动端保持单列和无横向溢出。
- 将画幅切换移至“01 任务说明”标题右侧；“02 创意定位”标题工具区仅保留展示类/叙事类切换，移动端同步保持紧凑布局。
- 固定“02 创意定位”类型切换工具组的右侧对齐，确保展示类与叙事类选中时控件位置和宽度一致。
- 统一叙事类与展示类标签交互：叙事类改为一个标签组一个章节，章节内按分类整理选项，并复用相同的主/辅选择与确认流程。
- 章节标题状态改为直接显示已选标签名称；确认最后一个可见章节后全部收起，不再循环打开首个章节。
- 展示类轮播支持固定 2～5 屏的逐轮创意定位：第 2 轮起默认继承第 1 轮，可切换自定义；AI决定模式保持一次最终生成并由 AI 统一决定屏数，结果展示轮次与 AI 补全标签。
- AI 网关遇到上游会话 401/403 时增加一次 Session Cookie 刷新重试；接口失败提示补充 HTTP 状态和排查方向。
- 启动器连接检测改为复用已健康的 8780 网关，避免重复进程导致控制令牌不一致，并记录脱敏错误详情。
- 撤回网页内网开放：启动器恢复绑定 `127.0.0.1:8775`，并移除 TCP 8775 局域网入站防火墙规则；AI 网关仍仅监听 `127.0.0.1:8780`。
- 按用户确认临时开放网页内网访问：网页绑定 `0.0.0.0:8775`，防火墙仅允许 `LocalSubnet`，AI 网关仍仅监听回环地址。
- 临时内网共享结束，网页恢复仅本机访问并删除 8775 局域网防火墙规则。
- 正式第 2 步标签编辑改为图 1 的报表式布局；每个标签组支持独立确认、摘要收起和修改展开，继续使用现有标签配置与数据字段。
- 报表编辑时已选选项从可选列表逐项移除，并以可移除的已选标签显示，避免重复选择。
- 新增独立网络代理工作台：支持固定 ISP 代理配置、出口 IP 测试、凭据脱敏和单独启动入口。
- 修复 SOCKS5/SOCKS5H 出口测试兼容性，并为无效代理端口增加安全提示。

## [0.1.0] - 2026-08-31

### Added

- 独立的 AI 创意工作台项目。
- 展示类与叙事类创意生成。
- 项目历史、方案采用、参考文件和展示类异步图片任务。
- 独立 `chat2api` 网关与 Windows 双击启动器。

### Known limitations

- 当前只支持本机单用户使用。
- 没有登录、权限、项目归属、自动备份或公网部署能力。
- 真实 AI 文字和图片生成尚未完成正式人工验收。
# 未发布

### Added

- 新增独立网络代理工作台，可配置固定 ISP 代理、测试出口 IP，并安全保存 `PROXY_URL`。
